# First 10 commercial bots — active launch memory

## HotelGPT qualification-strip removal (2026-09-25)

At the user's request, release `6c30642` removes the entire low-value qualification strip from the HotelGPT Pre-Stay inbox. It no longer presents a derived `62/100 · Warm` score or placeholder enquiry, stay, dates, budget and consent cards as if they were useful hotel-operating intelligence. The conversation header now flows directly into the suggested reply and guest conversation; the separate In-Stay operational summary and generic non-hotel qualification UI are unchanged. Local TypeScript, 269/269 channel tests and the complete 95-route Webpack build passed. The production-derived stage passed TypeScript, focused 8/8 and its complete 93-route Webpack build. Live/ready identify `6c30642`; PM2 is online with zero unstable restarts. Authenticated Chrome visually confirms the strip is absent in Camp Hornbill and the reservation number still renders as `+91 82796 40517`. Rollback: `/var/backups/aifrogi/inbox-cleanup-6c30642.uCszGM`.

## Hotel CRM labels and reservation phone formatting (2026-09-25)

At the user's request, release `ccf5240` replaces generic Pre-Stay CRM labels with `Guest enquiry qualification`, `Enquiry`, `Stay / cottage`, `Dates / guests`, `Stay or cottage preference pending` and `Stay dates and guest count pending` while leaving stored lead data unchanged. Public Indian phone numbers are displayed consistently as `+91 82796 40517`; raw ten-digit, `91`-prefixed and mixed-separator variants in governed guest answers are normalized before persistence and delivery. Local TypeScript, 269/269 channel tests and the complete 95-route Webpack build passed. The production-derived stage passed TypeScript, focused 8/8 and its complete 93-route Webpack build. Live/ready identify `ccf5240`; PM2 is online with zero unstable restarts. Authenticated Chrome confirms the new hotel CRM labels and a fresh guest question returned `For reservations, you can call The Camp Hornbill at: +91 82796 40517`. Rollback: `/var/backups/aifrogi/hotelcopy-ccf5240.v25YFU`.

## Camp Hornbill tenant-copy contamination repair (2026-09-25)

User requested an urgent repair after the live Pre-Stay inbox exposed Webtechnosys wording in Camp Hornbill. The production aggregate audit found no interlinked tenant data: Camp Hornbill had zero foreign-brand hits in approved knowledge and zero in stored conversation messages, and its runtime settings had zero foreign-brand references. The observed defect was shared-code contamination: the website-only staff suggestion hard-coded Webtechnosys. The audit also found and removed Webtechnosys from the shared prompt/secret refusal and Asavari Stays from reusable booking consent and generated booking URLs. Release `456eba0` now derives staff suggestions and safety refusals from the active tenant, uses connector-returned property URLs, uses the selected property for booking consent, renders legacy generic HotelGPT greetings as hotel-specific, and preserves explicit owner greetings. Camp Hornbill's stored welcome was changed from the generic business enquiry to `Welcome to Camp Hornbill. How can I help you plan your stay?`

Evidence: local TypeScript, 267/267 channel tests and a 95-route Webpack build passed. The production-derived stage passed TypeScript, focused 6/6 tenant-copy tests and its 93-route Webpack build using the production-generated Prisma client because the committed generated client still lags the deployed schema; unrelated generated Prisma work was preserved. Production live/ready identify `456eba0`, PM2 is online with zero unstable restarts, and the deployed contamination scan is clean. Authenticated Chrome shows the new hotel greeting in a fresh guest session and the staff suggestion now begins `Thanks for contacting The Camp Hornbill` with hotel-specific stay fields. Rollback: `/var/backups/aifrogi/tenantcopy-456eba0.uPVecN`.

## HotelGPT Quick Replies & Guest Updates deployment (2026-09-25)

The user explicitly approved deployment and full Camp Hornbill journey verification. Release `0a71cf1` deployed 18 versioned hospitality saved-reply defaults, independent tenant copies, separate Pre-Stay and In-Stay libraries, owner/admin management at Settings → Guest communication → Saved replies, contextual staff previews, explicit resolve-and-send completion, post-resolution feedback, clean department/time guest rendering and structured send audits. The automatic In-Stay acknowledgement is receipt-only. Camp Hornbill deliberately installed its independent defaults through the authenticated owner UI; no other tenant was activated.

Production-derived verification passed TypeScript, 254/254 committed channel tests and a complete 93-route Webpack build. Authenticated Chrome used synthetic room `E2E-15000`: the library installed, exact preview rendered, staff deliberately sent on-the-way and delayed updates, the guest received the new update as `Hotel team · time` without a repeated prefix, and Resolve & send changed the case to Resolved while delivering completion. Chrome then exposed that feedback sorted outside the four-action limit. Hotfix `c3df378` makes a resolved HotelGPT case expose only its feedback action; local and VPS focused regression passed 5/5, TypeScript passed and the complete 93-route VPS build passed before promotion. Production live/ready is healthy at `c3df378`, PM2 is online with zero unstable restarts and deployed feedback source is verified. Rollbacks: `/var/backups/aifrogi/quick-replies-0a71cf1-20260925.YzXNjq` and `/var/backups/aifrogi/feedback-c3df378-20260925.UL6ER8`.

After browser-control capacity returned, authenticated Chrome completed the remaining path. Reloading the resolved `E2E-15000` case showed only `Request feedback`; selecting it displayed the exact guest preview, a deliberate Send delivered `Was everything resolved to your satisfaction?`, and the guest rendered it as `Hotel team · time`. The guest’s existing resolution panel accepted `Yes, satisfied` and recorded `Feedback received: Positive`. The 430px guest frame remained centered, readable, vertically scrollable and free of horizontal overflow through completion and feedback. The full synthetic received → update → completion → feedback journey therefore passes. No real guest record was touched; only the existing synthetic `E2E-15000` journey and Camp Hornbill’s requested saved-reply installation were mutated.

## HotelGPT Quick Replies & Guest Updates candidate (2026-09-25)

The user approved a focused implementation of the final human-controlled HotelGPT communication layer. The local candidate is specified in `docs/HOTELGPT_QUICK_REPLIES_GUEST_UPDATES_SPEC.md` and adds 18 master hospitality defaults across separate Pre-Stay and verified In-Stay libraries. A hotel owner/admin can install an independent editable copy under Settings → Guest communication → Saved replies; later master changes never overwrite tenant copies. Staff see up to four contextual actions, the exact guest-facing preview and may edit before an explicit send. Operational completion uses a dedicated Resolve & send action, and feedback is offered only after resolution. Saved-reply sends audit the actor/time plus template/master identity, version, original approved text, final sent text, status, department and journey. Guest updates render a single department sender with time and strip duplicated legacy Front desk prefixes. The automatic resident acknowledgement was corrected to receipt-only language and no longer claims that resolution will start shortly. No PMS, schema migration, AI-written commitment, employee-name disclosure, automatic journey transition, tenant activation or deployment is included.

Local evidence: TypeScript passed; focused tests 21/21; complete channel suite 256/256; scoped lint has zero errors and four pre-existing guest-embed warnings; the 95-route Webpack production build passed. Chrome confirmed the new route is protected and redirects to login. Full authenticated Chrome journey and mobile verification remain pending because no local client password was available; no real hotel data was changed. Production remains at `047d6dc` for this product line until separate deployment approval. The exact pending scope is recorded in `docs/PENDING_VPS_CHANGES.md`.

Follow-up read-only production Chrome inspection used the user-open Camp Hornbill admin and guest tabs. The authenticated workspace is healthy enough to render three existing synthetic In-Stay cases and the verified guest state, but it conclusively remains on the old UI/runtime: Settings lacks Guest communication, and room `E2E-15000` retains the acknowledgement that says resolution will start shortly. The guest tab correctly remains at “Request sent · Awaiting front desk update.” No production mutation was made. This establishes the before-deployment baseline; it is not verification of the local candidate.

## HotelGPT Flow Intelligence foundation deployment (2026-09-25)

The user approved the agreed three-layer HotelGPT architecture and its safe end-to-end deployment. The permanent specification is `docs/HOTELGPT_FLOW_INTELLIGENCE_ARCHITECTURE.md`. Release `bd622ab` added the versioned Super Admin visual library with six hotel-specific defaults: Discover the Hotel, Find the Right Stay and Plan Arrival for public Pre-Stay; Request a Service, Report a Problem and Resolution & Feedback for verified In-Stay. A hotel owner can install independent tenant-owned draft copies from Flow Intelligence, edit them without changing the master library or other hotels, and publish only after existing journey validation passes. Publishing cannot place an In-Stay flow in the public website menu. Runtime matching is constrained to the active journey and adds structured smart-content status and quick replies without bypassing approved knowledge, connector or human-handoff boundaries. Existing tenants, published flows and menus remain unchanged until an owner deliberately installs and publishes a flow. No PMS integration, automatic phase transition, database migration or silent global activation was added.

During production Chrome verification, Pre-Stay answered a live Camp Hornbill discovery question correctly and showed the three-dot reply state. The first verified-room In-Stay request exposed a pre-existing resolved-case recovery defect: the guest UI reopened the room case, but the API tried to create a second lead and returned a persistence error. Hotfix `047d6dc` now reuses the approved stay's durable room lead, clears only resolved/closed tags, reopens the case as `NEW`, renews its visitor session and retains the stay/room boundary. The exact room `E2E-15000` replay then passed in Chrome: the request was accepted, the governed acknowledgement appeared and the status changed to awaiting the front desk. Local validation passed TypeScript, focused 21/21 and complete 256/256 channel tests. VPS stages passed TypeScript, 254/254 for the main release, focused 21/21 for the hotfix and complete 92-route builds. Public readiness is green at `047d6dc`, PM2 is online with zero unstable restarts, deployed route hash matches the committed source, public Pre-Stay/In-Stay pages return 200 and protected staff/admin routes return 307 without authentication. Rollbacks: `/var/backups/aifrogi/hotel-flow-bd622ab-20260925.KTNHbW` and `/var/backups/aifrogi/instay-reopen-047d6dc-20260925.Vs9nN6`. The Super Admin Chrome library passed. The hotel-owner install/publish click path was not mutated in production because the saved browser lacked client credentials; its API/UI boundary remains covered by tests.

## HotelGPT operational ownership candidate (2026-09-25)

After reviewing the remaining product shortcomings, the user approved implementation and subsequently deployment of the focused stability scope without PMS integration or automatic journey-phase complexity. Release `0a5a7dd` closes the most immediate In-Stay operating gap: every case is now room-first, carries a durable department and staff owner, and exposes configured SLA state as on-track, due soon or overdue. Front Desk, Housekeeping, Food & Beverage, Maintenance and Safari & Experiences routing is saved on the existing tenant-bound lead through reserved operational tags; inferred routing remains the backward-compatible default until a staff member confirms it. `Start work` also claims the case, a separate `Take ownership` action is available, and the owner dashboard now highlights open, unassigned and overdue work. Agents, admins and owners can operate cases; viewers remain read-only. No schema migration, PMS integration, bot intelligence, guest UI, payment configuration or tenant knowledge was changed. Local TypeScript, focused dashboard/SLA/In-Stay tests (9/9), scoped lint and the complete 94-route Webpack production build passed. The production-derived VPS stage repeated TypeScript, 9/9 tests and its complete 92-route Webpack build before promotion. Public live/ready are green at `0a5a7dd`; PM2 is online with zero unstable restarts; protected In-Stay, In-Stay Inbox and Pre-Stay routes redirect unauthenticated visitors, the cases API returns 401 without authentication, and Camp Hornbill's guest stay route returns 200. Deployed source hashes match the verified stage. Rollback: `/var/backups/aifrogi/operations-ownership-0a5a7dd-20260925.uFc15O`. The exact VPS stage/archive were removed after verification.

## HotelGPT lightweight guest UI deployment (2026-09-25)

User approved a local UI refresh for both Pre-Stay and In-Stay after supplying three conversational hotel-journey reference videos, then explicitly rejected carrying forward the earlier card/dashboard design. The revised candidate follows the videos' lightweight conversation pattern: a compact hotel identity and online state, generous breathing room, one welcome message near the composer, and quick actions that progressively become a guest message before the bot continues. It removes the permanent Explore/Chat switch and 2-column action grid. A subsequent creative pass, guided by the user's chatbot reference image, adds an asymmetric digital-concierge bubble, a restrained accent gradient, hospitality-specific microcopy and compact outlined quick-reply pills with one stronger primary action; it deliberately avoids returning to a heavy card interface. On desktop, the HotelGPT embed is centered in a 430×760 maximum chatbot frame instead of stretching across the viewport; at 520px and below it becomes a borderless full-screen 100dvh experience. The header now provides separate guest-options and accent-coloured human-transfer controls; transfer prepares the existing unsent human-help request instead of sending automatically. Setup → Bot appearance retains its custom colour picker and now adds six accessible hotel palette presets; saved colour drives the guest shell, concierge avatar, actions, transfer button and send control with automatic contrast. Existing APIs, tenant knowledge, access approval, handover and request logic remain unchanged. Pre-Stay is labelled “Plan your stay”; In-Stay is labelled “During your stay” and remains room/stay-token isolated. Hotel standalone pages no longer show staff-facing install/share controls or the dark generic background. A development-only preview supports both journey states without production data. TypeScript and scoped lint pass (existing warnings only); focused channel/security tests pass 17/17 and the complete 94-route local Webpack build passes. Approved commits `f3abb4a` and production-check hotfix `ffdf937` were pushed to `main` and deployed through a production-derived stage. The VPS repeated TypeScript, 17/17 tests and its complete 92-route build. Public live/ready are healthy at `ffdf937`; PM2 is online with zero unstable restarts. Camp Hornbill Pre-Stay, embed and In-Stay entry routes return 200. Live Chrome at 430px confirmed the new Pre-Stay shell, working human-transfer control, aligned composer, no overflow and suppression of the inactive menu icon when the tenant has no saved Pre-Stay menu; the Camp Hornbill In-Stay login/registration entry is also responsive. Asavaristays Pre-Stay is live, while its unconfigured In-Stay route correctly remains 404. Rollbacks: `/var/backups/aifrogi/hotelgpt-ui-f3abb4a-20260925` and `/var/backups/aifrogi/hotelgpt-ui-ffdf937-20260925`. Temporary VPS staging artifacts were removed.

Follow-up after the user reported that Pre-Stay was not replying and warned against mixing journeys: the server paths were already separated by the presence of a signed In-Stay capability, and a controlled live Pre-Stay request returned HTTP 200 with a governed reservations answer. The browser, however, still reused the legacy generic visitor storage key for Pre-Stay, which could restore an older human-owned or closed conversation into the redesigned bot. Release `236553d` gives Pre-Stay its own `aifrogi-prestay:<slug>` namespace; In-Stay remains isolated by its opaque signed-stay suffix and generic bots retain their original key. A regression assertion now covers all three namespaces. Local and VPS TypeScript passed, focused tests passed 18/18, and complete builds passed (94 local routes; 92 production-derived routes). Public ready/live identify `236553d`, PM2 is online with zero unstable restarts, a fresh synthetic Camp Hornbill Pre-Stay turn answered with HTTP 200, and both `/bot/camp-hornbill-0c9ec7` and `/stay/camp-hornbill-0c9ec7` return 200. Rollback: `/var/backups/aifrogi/journey-isolation-236553d-20260925`. Temporary staging artifacts were removed.

## Camp Hornbill In-Stay menu restoration after VPS reset (2026-09-24)

After the VPS cleanup/reset, the server retained release label `f110643` but restored an inconsistent older source/build mix: the guest bot showed only chat and the old resolution panel, while the committed six-option In-Stay menu was absent. User approved restoration. Five committed files were restored from `f110643`: the resident entry, shared bot embed, menu navigation, guest stay page and `INFO` menu type. The isolated production-derived candidate passed TypeScript and the complete 92-page build. Public live/ready are healthy, deployed hashes match the commit, PM2 `lead-os-ai` is online with zero unstable restarts, and authenticated Chrome confirms Safari, Experiences, Booking assistance, General information, Request service and Report complaint. Safari opens inside the bot; Back returns to the menu; Request service opens Chat with the correct unsent request prefix. Rollback: `/var/backups/aifrogi/menu-restore-f110643-20260924`. Temporary build stage removed; VPS retains about 59 GB free.

## Camp Hornbill Team Inbox journey-selector correction (2026-09-24)

User confirmed that switching between Pre-Stay and In-Stay failed. Live Chrome reproduced the exact state: Camp Hornbill had three In-Stay conversations and no Pre-Stay conversations; selecting the empty Pre-Stay view triggered the global empty return before the journey selector rendered, trapping staff outside In-Stay. Release `f110643` keeps the selector visible in an empty journey and labels the empty state by journey. TypeScript, focused 4/4 tests, scoped component lint and the production-derived 92-page build passed. Public live/ready are healthy at `f110643`; PM2 is online with zero unstable restarts and the deployed source hash matches the candidate. Authenticated Chrome passed In-Stay → Pre-Stay empty state → In-Stay and restored all three conversations. Rollback: `/var/backups/aifrogi/team-inbox-f110643-20260924`. VPS cleanup removed only three obsolete `/var/tmp` build stages and recovered about 4.1 GB; live data, environment, uploads and rollback packages were preserved.

## Camp Hornbill In-stay module (2026-09-24)

User approved adding In-stay under Manage and deploying it for Camp Hornbill. The local candidate adds a HotelGPT-only `/in-stay` workspace with a live-state indicator, governed guest-access explanation, high-correction QR generated from the public bot URL, downloadable PNG, guest-view test link and hotel/property/privacy/connector boundaries. The QR contains no password, OTP or private credential and reuses the same published owner-approved Intelligence. Deployment verification is pending.

## Homepage mobile hero correction (2026-09-24)

User reported a white rectangle covering the homepage hero on iPhone Safari. The artifact originated from the mobile compositing path for the hero video's `mix-blend-mode: screen` layer. The correction keeps the existing poster artwork on phones and reduced-motion devices as an optimized static image, does not load the video source below 640px, and retains the existing one-shot blended animation on larger screens. Homepage copy, CTAs, bot behavior and tenant knowledge were not changed. Scoped commit `4631646` was pushed and deployed after explicit approval. Local TypeScript, focused component lint, homepage regression 5/5 and the 91-route Webpack build passed; the isolated production-derived stage repeated TypeScript, 5/5 and its 91-route build. The required dashboard invariant held at 6 organizations, 465 website conversations, 14 subscriptions and 2 credit transactions; AI Readiness quarantine, Core 30/30, fleet Golden 3/3 and SEC-001 passed. Public live/ready identify `4631646`, homepage is HTTP 200 with the poster present, deployed sources match the candidate, and PM2 is online with zero unstable restarts. TypeSafe mode and gateway remain off. Rollback: `/var/backups/aifrogi/homepage-mobile-4631646-20260924`. Final physical-device confirmation should repeat the original iPhone Safari view.

## Dashboard refresh performance deployment (2026-09-23)

User approved deployment of the narrow Team Inbox dashboard performance package. Release `885c2a4` removes unconditional full server-page refreshes every 10 seconds from the Team Inbox clients, retains the lightweight 20-second summary poll, pauses that poll while the tab is hidden, refreshes server data only when unread or human-help counts change, and preserves immediate refresh after staff actions. Local TypeScript, focused tests, channels 247/247, lint and the complete build passed. The isolated VPS typecheck and 89-route production build passed before promotion. Live/ready are healthy at `885c2a4`, public dashboard and Team Inbox retain their unauthenticated 307 protection, PM2 is online with zero unstable restarts, promoted source hashes match locally, and all TypeSafe flags remain off. Rollback: `/var/backups/aifrogi/dashboard-performance-885c2a4-20260923`. No database, bot intelligence, billing, public frontend or tenant knowledge was changed. Next action must be chosen by the user; do not expand scope autonomously.

## Dashboard data-context recovery candidate (2026-09-23)

User approved repairing the system after authenticated Chrome review showed blank client Billing and an empty Team Inbox despite intact production records. Root cause was repeated reliance on database identity established during authentication, which does not reliably survive Next.js page/API async boundaries. Release `9d4a451` adds an explicit reusable client tenant boundary and applies explicit tenant/platform authority to client Billing, Team Inbox page and summary read/write API, Admin Billing/Matrix, Analytics, Contacts, Flow Intelligence, Improve My Bot, Team settings, Support, plan checkout and verified plan/credit activation. It does not change bot intelligence, TypeSafe, public frontend, credits, customer data or payment credentials. Existing Webtechnosys conversations required no migration. Verification passed: focused security 9/9, channels 241/241, production-hardening 127/127, TypeScript, scoped lint and complete local 91-route plus production-derived 89-route Webpack builds. The clean Git candidate exposed a pre-existing mismatch between committed and live generated Prisma artifacts, so promotion used the established production-derived candidate with only the 16 approved files overlaid. Production live/ready are healthy at release `9d4a451`, PM2 is online with zero unstable restarts, all TypeSafe flags remain off and Asavaristays embed is HTTP 200. Authenticated Chrome confirms client Billing now renders the trial, 801 remaining credits and history; Team Inbox now renders all 227 Webtechnosys conversations, 35 messages since review and 26 human-help requests. Rollback: `/var/backups/aifrogi/dashboard-data-context-9d4a451-20260923`. The Razorpay webhook secret remains missing and unchanged; do not invent or configure it without explicit approval and the real provider secret.

## Dashboard data and Core workflow candidate (2026-09-23)

User approved restoring missing Admin and client dashboard data and adding a read-only admin visual workflow for Core Intelligence. Root cause: database authority was established indirectly in layouts/session helpers, but Next.js can render layouts and pages in separate async contexts; RLS therefore returned empty datasets on affected pages. The local candidate now establishes explicit platform authority around Admin Command Center, Customers and Intelligence Operations queries, and explicit tenant authority around the client dashboard query group. Intelligence Operations renders the actual five Core frames as a selectable seven-node detect → tenant → property ID → evidence → authority → compose → verify workflow. It is deliberately read-only; client facts remain tenant/property isolated, TypeSafe remains off and the marketing frontend is unchanged. Verification: focused 32/32, channel suite 240/240, TypeScript, lint for changed application files, and the complete 91-route Webpack production build pass. Turbopack could not bind an internal sandbox port, so Webpack was used as the production build verifier. Local UI route reached authenticated login; no credentials were used, so production data rendering still requires post-deployment authenticated verification. This candidate is not deployed. Next action: obtain separate deployment approval, then verify actual Admin customer counts, client workspace metrics and the read-only Core workflow while authenticated.

Deployment approved and completed as release `9115d4e`. The production-derived candidate passed TypeScript, its complete 89-route Webpack build, Core 30/30, Golden certification for all 3/3 live tenants and SEC-001. Rollback is `/var/backups/aifrogi/dashboard-core-9115d4e-20260923`. Live/ready are healthy, Asavaristays embed is HTTP 200, PM2 is online with zero unstable restarts, and every TypeSafe runtime flag is off. A platform-authority read-only database check confirms five non-demo customer records and real workspace data (Webtechnosys 227 leads/299 evidence records, Asavaristays 38/140, Castle Mandawa 13/57), proving the dashboards no longer receive an empty RLS result. Admin and client routes correctly redirect unauthenticated requests; final visual verification still requires an authenticated session. Public website frontend was not changed.

## Property-ID Core recovery candidate (2026-09-23)

After CI-025 deployment, the user approved Chrome testing and a same-question recheck. Five failures were repeatable, including the critical leak of Kate’s Adobe’s ₹22,500 rate into an explicit Rohet Garh answer. The user then approved a reusable n8n-style Core correction based on stable property IDs for future HotelGPT scale. `CI-026` is implemented locally only: detect topics → resolve tenant → resolve property ID → retrieve property-scoped evidence → authority → compose → verify. It adds cross-property source blocking, explicit correction isolation from stale rate context, bedrooms/bathrooms multipart facts, exact requested-landmark handling, previous-guest privacy refusal and complete-fallback enforcement. Focused 17/17, Sovereign 391/391, production-hardening 94/94, channels 231/231, TypeScript and the 91-route Webpack build pass. TypeSafe remains off. No tenant facts, frontend, credits, connector authority or production system changed. Next action requires the user’s separate deployment approval; after deployment, repeat only the five failed Chrome questions before considering any additional work.

The user separately approved deployment and live Chrome verification. `CI-026` is live as `5a47db6`, with rollback `/var/backups/aifrogi/ci026-5a47db6-20260923`; release gates, public health and TypeSafe-off checks passed. The critical Kate’s-to-Rohet rate leak is removed. Exact landmark handling passed, and visual inspection showed the cancellation answer is complete despite accessibility-text clipping. Two answer gaps remain: exact property ingestion lacks all requested structured fields for the Kate’s multipart and Rohet rate/capacity questions, so both safely hand over; and the previous-guest request reveals no guest data but the route discards the explicit REFUSE disposition and serves generic handover. Do not correct either without first presenting these findings and obtaining user approval.

## Core answer recovery candidate (2026-09-23)

User approved local implementation and then separately approved production-derived Golden verification; deployment was not approved. Commit `17e266c` implements shared Core context priority, explicit-entity deep context, current-question operation slots, percent-spacing equivalence, unsupported entitlement blocking, and an internal retrieval/proposed/final answer replay trace. Tenant knowledge, website frontend, TypeSafe settings and credits were unchanged. Local verification passed focused 10/10, TypeScript, Sovereign 381/381, channels 232/232, production-hardening 126/126 and a 91-page webpack build. Isolated production-derived candidate `/var/tmp/aifrogi-ci023-17e266c` passed TypeScript, its complete 89-route build, Core 30/30 and fresh Golden certification for all 3/3 live tenants.

Do not treat the 3/3 result as answer-quality acceptance: the current certification record saves dispositions rather than answer text, and Asavaristays' Golden bank does not contain the four observed regression topics (Kates rate, parking charge, general online booking and cancellation). Before recommending deployment, replay the same ten Asavaristays questions against the isolated candidate and review the exact answers, then run the security gate. Obtain explicit approval before that replay if it will make additional model calls, and separate explicit approval before deployment.

The user approved that isolated ten-question replay. Exact-answer results: identity and all eight destinations were correct; Kates Adobe returned INR 22,500 plus 18% tax; capacity returned 10; Coorg returned Wild Cat Coorg and Sagar Estate; breakfast was described as not included by default; general online booking selected no inherited destination; the cancellation policy page was retrieved; and unverified booking confirmation escalated. Parking remains wrong: the answer said self-parking was “included as a property amenity,” while the approved source does not explicitly establish no charge. Stop here. Do not deploy or broaden work. Ask for approval for one narrow shared entitlement-validator correction, then repeat the isolated replay and security gate before requesting separate deployment approval.

The user approved the narrow entitlement correction and separately approved the two parser fixes revealed by the next replay: negated “not included” is not a positive entitlement, and “incurs 50%” is not parsed as an `rs 50%` currency claim. Final exact-answer replay passed the intended ten-topic review. Parking is now withheld because free/no-charge evidence is absent; breakfast and cancellation remain answerable. Final local evidence: focused 7/7, TypeScript, Sovereign 384/384 and production-hardening 126/126. The exact production-derived candidate passed its complete 89-route build and SEC-001 (3 live tenants, no invalid credentials, no duplicate owners). Nothing is deployed. Ask for explicit deployment approval; if granted, deploy only CI-023 and retain rollback, with frontend, tenant knowledge, TypeSafe and credits unchanged.

The user explicitly approved deployment after confirming that the backup is only a rollback safeguard. CI-023 is live as release `c614c9b`. The previous build, affected sources and environment are preserved at `/var/backups/aifrogi/ci023-core-recovery-c614c9b-20260923`. Public live/ready are healthy and identify `c614c9b`; Asavaristays embed returns 200; PM2 is online with zero unstable restarts; promoted hashes match the verified candidate; no new error-log entry appeared after restart. TypeSafe remains fully off. Frontend, tenant knowledge and credits were unchanged. Next work requires the user to choose it; do not autonomously expand scope.

## Asavaristays bot retest deployment (2026-09-23)

User approved deploying the targeted bot-answer fix, granting exactly 500 Asavaristays reply credits, and repeating the same ten Chrome questions; no other work was authorized. Commit `6073810` changes only the public bot route plus a small stay-question helper and its tests; website frontend is unchanged. The isolated production-derived candidate at `/var/tmp/aifrogi-botfix-6073810.lBcZcp` passed generated-Prisma TypeScript, 126 production-hardening tests, and a Webpack production build. An initial release-gate failure was caused by missing runtime files in the isolated stage, not missing live certification: all three live tenants already had 10-case passed Golden banks. Copying runtime records into the stage yielded Core 30/30 and Golden 3/3; the security inventory passed when run with the protected production environment. No certification was fabricated or waived. Promoted only the bot route, new helper, and build as release `6073810`, preserving the old route and build at `/var/backups/aifrogi/botfix-6073810`. PM2 online; live/ready and Asavaristays embed all HTTP 200; TypeSafe remains off. Commit was pushed to `origin/main`.

Granted exactly 500 non-expiring free QA reply credits to Asavaristays, transaction `cmudi8rcx0000wgkxs5gk1ewd`, with audit reason identifying the user-approved ten-question retest. Chrome retest, same questions in one session: (1) brand identity now correctly explained; (2) all eight approved menu destinations listed; (3) Kates Adobe rate still not answered despite public starting price ₹22,500/night, with human handover; (4) capacity 10 answered, though the "Cottage Room" label should not be assumed verified; (5) Coorg correctly named Wild Cat Coorg and Sagar Estate instead of Mukteshwar; (6) parking called free/included without explicit proof of no charge; (7) breakfast described as optional on EP rate; (8) online booking opened the right panel but wrongly preselected Coorg from earlier context and did not give a clear general booking answer; (9) cancellation policy incorrectly said unavailable though the website links to a published policy; (10) booking-without-payment boundary correctly declined unverified confirmation. No reservation or payment was made, and no contact details were submitted. Do not make further fixes, credits, or rollout changes until the user chooses the next action.

## Current rollback state (2026-09-23)

15 September rollback investigation: user approved a code-only rollback to end-of-day commit `5239c7d` while retaining current data. That exact source was staged separately on Core, passed TypeScript and a production build, and returned live/ready 200 when run with current PM2 environment. However, the staged Asavaristays embed returned 404 while current production returned 200. Later tenant database-context/security changes are absent in the 15 September route, so the old code is not compatible with the current secured database. The isolated test server was stopped; production remains `0700fa8`. Do not promote the 15 September build or restore the database without a new decision. The previously observed exhausted reply credits would also remain under a code-only rollback. Next action: ask the user whether they want a narrowly adapted 15 September bot-behavior rollback with current security plumbing preserved, or no further rollback.

After the user approved a narrow bot-behavior rollback while keeping the website frontend unchanged, source comparison showed that the 15 September booking/answer logic is already present in the current release. `git diff 5239c7d..HEAD` for the public bot route contains tenant database-context wrapping and disabled TypeSafe instrumentation, not a newer booking-answer implementation. The booking planner's broad `stay/stays` trigger is present in the 15 September baseline and can misroute “What is Asavari Stays?”; reverting cannot correct it. No code, frontend, or production change was made under this no-op rollback. A targeted bot-answer fix and separate credit decision require explicit user approval; do not claim this rollback improved responses.

At the user's request after the ten-question Asavaristays test, Core production was restored to the pre-TypeSafe build from release `0700fa8`. PM2 release is `0700fa8`; `TYPESAFE_ACTION_GATEWAY_ENABLED=false`, `TYPESAFE_MODE=off`, `TYPESAFE_HOTEL_SHADOW_ENABLED=false`, `TYPESAFE_PRE_SEND_ENABLED=false`, and staging routing is false. The replaced build is preserved at `/var/backups/aifrogi/typesafe-rollback-from-3321ec0-20260923`. Production live/ready health and the Asavaristays embed returned HTTP 200. No TypeSafe policy records or historical observations were deleted. Do not re-enable TypeSafe or choose another task without the user's permission.

Follow-up at the user's renewed rollback request: live PM2 and health were rechecked and still matched the pre-TypeSafe runtime above. The five later hotel TypeSafe commits were reverted in source as `dfddfa1` and pushed to `origin/main`; the committed source tree now equals `0700fa8`. TypeScript and 67 TypeSafe baseline tests passed. Existing unrelated dirty/untracked work was preserved. The bot's previously observed answer-quality failures were not repaired by this rollback; await the user's specific symptom and approval before separate fixes.

## Current user approval and decision rule (2026-09-23)

Before autonomously choosing any next work, changing scope, or deploying unapproved work, inform the user and obtain permission. The user chose Option 1: a TypeSafe before-send quality check for the two live HotelGPT bots, then explicitly approved deployment and chose every eligible answer, without an AiFrogi expiry timer or daily cap. Only a high-confidence factual conflict with exact approved hotel knowledge may change the answer to the existing staff-handover response. TypeSafe failure leaves the current bot answer unchanged. Do not extend to other bots, booking/payment authority, or other work without asking.

Current deployed release `3321ec0` (2026-09-23) has `TYPESAFE_PRE_SEND_ENABLED=true` on Core production. Asavaristays and Castle Mandawa have active policies with `expiresAt:null` and `dailyLimit:null`; no other hotel was activated. Build, TypeScript, lint, and 133 production-hardening tests passed. Production live/ready health returned 200/200; super-admin TypeSafe page remained login-protected. Two synthetic cases sent through the deployed module returned the expected contradicted/support outcomes. No guest conversation was created for this smoke check. The matrix had zero live quality observations at verification, so actual end-to-end guest and staff-handover behavior remains unmeasured. No new task or wider rollout is authorized by this deployment.

Previous TypeSafe release `22e2144` deployed an inactive hotel shadow control/usage table before this correction; gateway remained off. The user rejected that direction. Replace its per-hotel cost/renewal emphasis with the approved quality matrix and API-health view; do not present it as completed answer-quality work.

Approved TypeSafe work completed 2026-09-23: release `c80a66d` is deployed on Core production. It checks eligible saved HotelGPT answers asynchronously, linked to the exact answer evidence ID; it cannot change replies, bookings, payments, or knowledge. Super admin sees an aggregate seven-day answer-quality matrix and provider API health, with no separate client TypeSafe cost. Eight synthetic labelled provider cases scored 8/8 answer-fit and 7/8 grounding; 73 TypeSafe and 129 production-hardening tests passed. Health live/ready both returned 200 after deployment. Shadow gateway is on for Asavaristays and Castle Mandawa only, each capped at 20 attempts/day and expiring 2026-09-24 00:19 UTC (05:49 IST). No live quality observations existed at handoff, so do not claim measured production quality. The provider does not return an API-key renewal date; check the provider account. Do not renew these policies, expand tenants, or take a new task without first informing the user and obtaining permission. Older notes below describing TypeSafe as off are historical baseline, not current state.

Recorded 2026-09-22 at the user's request to prevent drift and complete the task.

## Objective and working rule

Complete AiFrogi's first 10 commercial bot launches. Prioritize launch blockers over feature expansion. Every work item must identify which launch acceptance criterion it serves. Keep a single evidence-backed blocker list; finish the current blocker before adding optional work. Do not repeatedly reopen settled scope or repeat checks without a change or unresolved concern.

This document preserves project direction; it is not approval for deployments, customer communications, charges, new live sampling, or changes to customer-approved knowledge. Follow the authorization established in the conversation for those actions.

## Camp Hornbill owner onboarding correction (2026-09-24)

The owner onboarding exposed multiple tenant-context and UX defects. Releases `7047679`, `1b6cf75`, `54126b8`, `5cf2296`, `f357dc1`, and the generic redesign `2b1b5f1` are deployed. New-client onboarding is now three sequential screens: business details; upload/confirm workbook with the website crawl attempted inside that same step; submit to Super Admin. A failed or JavaScript-only crawl is handed to Super Admin and never blocks client submission. Submission creates a high-priority `ONBOARD-<organizationId>` review ticket. Pre-approval clients remain in onboarding and cannot open full workspace resources; LIVE/PAUSED clients can. Super Admin retains all knowledge, testing, certification and go-live gates. Camp Hornbill was not submitted or made live as part of the redesign. Production is healthy at `2b1b5f1`; rollback `/var/backups/aifrogi/simple-onboarding-2b1b5f1-20260924`.

## Launch scope

- Approved business answers, consented lead capture, human handover, conversation history, and basic reporting.
- Booking/payment capabilities only for a customer whose specific integration has been verified.
- Repeatable onboarding, customer approval, commercial billing lifecycle, and accountable support.
- Core bot product only. AI Readiness billing/infrastructure remains separate. Defer new channels, redesigns, speculative features, and unrelated refactors unless required by a demonstrated launch blocker.
- TypeSafe remains off. Its evidence and staging defects are separate follow-up work unless they affect the core launch. Do not renew its pilot or activate routing automatically.

## Fixed sequence

1. Audit Asavaristays, Castle Mandawa, and Webtechnosys against the acceptance criteria below. Produce one prioritized blocker list from observed failures.
2. Complete and verify the launch-critical fixes, preserving unrelated dirty/untracked work.
3. Verify repeatable onboarding and the core commercial billing lifecycle: plan, collection, activation, renewal, cancellation, invoice, and expiry behavior.
4. Obtain the required customer approvals and launch the first batch of three when authorized.
5. Review actual conversation and operational results; address failures before adding the next three, then the final four.
6. Record evidence and approval for all ten. Do not claim ten commercial launches based only on code completion, synthetic tests, or HTTP availability.

## Per-bot completion criteria

- Identified customer, named business approver, and accountable support owner.
- Customer-approved knowledge and representative answer tests.
- Complete enquiry → answer → consented lead capture → handover → staff response journey verified.
- Lead delivery, mobile experience, privacy, and tenant isolation verified.
- Applicable billing lifecycle verified; any booking/payment integration separately verified.
- Disable/rollback procedure tested and release recorded.
- Customer launch approval and actual launch status recorded.
- Post-launch review tracks incorrect answers, missed leads, failed handovers, latency, and operating cost.

Maintain a roster of the ten real customers as they are identified. Currently known bot candidates: Asavaristays, Castle Mandawa, Webtechnosys. The other seven and commercial acceptance for all ten are not yet established in this task.

## Verified baseline and limitations

- Canonical repository: `/Users/manishpurohit/Documents/ChatGPT/New project/aifrogi-canonical`; main at `9ba19c8` during the 22 September audit.
- Core production: `187.77.188.146`, `/var/www/lead-os-ai`, PM2 `lead-os-ai`, port 3011. The live directory is not a Git checkout.
- AI Readiness VPS `187.77.191.216` must remain separate; do not mix deployments, databases, or secrets.
- Six TypeSafe-critical production source hashes matched committed source. Pilot status was off with one observation and zero human reviews. All 67 local TypeSafe tests passed; this is not core launch certification.
- TypeSafe report joins observations to the newest evidence in a session, which can show the wrong turn. Exact transmitted input is not established. Do not assign a correctness verdict from that linkage.
- TypeSafe admin promotion reporting hard-codes some PASS claims; staging routing lacks an independently verified environment boundary. Keep activation blocked pending evidence and fixes.
- Correction: 38/40 synthetic cases equals 95% and meets the numerical accuracy threshold; other required evidence and independent review remain missing.
- Earlier Core 30/30 and Golden 3/3 checks are historical; full customer journeys and current commercial acceptance remain to be verified.
- Preserve generated Prisma changes, next-env.d.ts, pending Readiness billing files, the live-budget verification script, and untracked artifacts. Do not blanket stage or deploy them.

## Next action

Latest user steering: "just smoke test the project, if required else we will launch bot and find out issue". Limit this pass to smoke verification; do not expand into a full certification project or additional feature work. Support an initial launch and address observed issues as reported. The user identifies themselves as admin and launch/support owner. The remaining seven customers are still unspecified.

Smoke pass completed 22 September 2026:
- Production readiness, login, and all three public bot pages returned HTTP 200; release remains 9ba19c8.
- Live security inventory passed: three live tenants, zero invalid live connector credentials, zero duplicate owner identities. TypeSafe remains off.
- Read-only operational inventory: no unresolved incidents and no dead automation jobs for the listed tenants. The three live bots have handover enabled and active member records.
- All three live bots are still TRIALING with zero paid invoices. Trial expiry: Asavaristays 25 September, Castle Mandawa 30 September, Webtechnosys 4 October. Castle has no installation-detected timestamp; this does not prove that the public bot is unavailable.
- Local core tests: 653 passed across four suites (538 + 35 + 40 + 40). Commercial readiness tests: 5 passed. TypeScript check passed.
- These checks did not submit live customer conversations, charge customers, or establish complete real staff-reply/mobile acceptance.

Local work prepared before the user narrowed scope: `lib/commercial-launch-checks.ts`, changes to `scripts/verify-commercial-launch.ts`, `tests/commercial-readiness/launch-inventory.test.ts`, and `ops/audit-commercial-bots.mjs`. The verifier now reads tenant inventory under transaction-local read-only authority and blocks empty live inventory, severe/unclassified incidents, and dead jobs. Tests passed; changes remain uncommitted and undeployed. The audit script was executed over SSH stdin without installing it remotely. Existing unrelated changes were preserved.

Next action: user-led initial launch/use of the existing bots, followed by targeted fixes for observed issues. Do not claim ten commercial launches are complete. Do not silently extend trials or activate paid plans.

## Handoff discipline

HotelGPT-first Core package (2026-09-23): after review and explicit approval, `CI-024` was deployed as release `a30eccd`. It passed 60/60 HotelGPT-first cases, TypeScript, Sovereign 386/386, production-hardening 126/126, channels 232/232, the 89-route production build, Core 30/30, live tenants 3/3 and SEC-001. Public readiness and the Asavaristays embed are healthy. Rollback: `/var/backups/aifrogi/ci024-a30eccd-20260923`. Changes are limited to shared typo/context/privacy/password/incomplete-answer behavior; tenant knowledge, frontend, credits and TypeSafe remain unchanged/off. Do not choose another task or expand this package without informing the user and obtaining permission.

Approved TypeSafe Option 1 experiment (2026-09-23): ran the same ten Asavaristays questions through Core and an isolated answer-quality shadow comparator. No guest answer, conversation, production flag or route was changed. TypeSafe handled simple identity/directory cases but was unreliable on the full bank: low-confidence partial labels for correct rate and capacity, privacy-filter skip of the safe parking fallback, and a cancellation grounding warning caused by incomplete evidence projection. Do not enable live TypeSafe shadow from this result. Production stays on Core release `a30eccd` with all TypeSafe flags off; no rollback was needed.

Framed Core Intelligence package (2026-09-23): user approved local implementation and then directed that multipart reasoning use n8n-style framed templates. `CI-025` provides five reusable detect → resolve → retrieve → authority → compose → verify frames, exact published rate/capacity answers, multipart completeness enforcement, stronger property typo resolution, policy/booking-state guidance, a 20-turn HotelGPT journey, and a deterministic Super Admin quality score. Local focused 16/16, journey 20/20, Sovereign 390/390, production-hardening 127/127, channels 233/233, TypeScript and the 91-route Webpack production build passed. TypeSafe remains off; tenant knowledge, credits and marketing frontend are unchanged. This package is not deployed and needs separate explicit deployment approval.

User-authorized visual change after the smoke pass: implement a subtle homepage hero mouse tilt, gold glow, and entrance animation. Completed locally in `components/marketing/sovereign-hero.tsx` and its CSS module. No animation dependency added. Desktop pointer movement verified in the browser; 390px mobile rendering has no overflow and the visual is static. Reduced-motion media rules disable the effect. TypeScript and component lint passed. Preview runs at http://127.0.0.1:3117; this change is uncommitted and undeployed. This specific request does not expand the broader launch scope.

Record completed work with evidence, outstanding blockers, and the next action. Distinguish verified, historical, pending, and blocked states. Never invent a readiness rating, customer approval, security guarantee, or completion claim. Keep updates concise and token-efficient.

Current hero result: user rejected the added SVG orbit as visually unsuitable. Removed it completely and kept the original PNG ring. Added a gentle seven-second float/sway to the original artwork on mouse-capable screens at least 640px wide; desktop pointer tilt remains. Small mobile screens and reduced-motion preferences keep the artwork static. Browser verification confirms no SVG orbit remains and the float is running. Still local and undeployed.

Superseding hero request: use only the starting bot/logo from user-supplied `hf_20260906_062652_6b684b27-7b33-45e0-b232-4c024f270323.mp4`. Implemented first 1.75 seconds forward/reverse as a 3.5-second silent loop (187 KiB), excluding all later phone/text scenes. `HeroVideo` replaces pointer/float effects, includes a matching poster, pause control, autoplay fallback, and reduced-motion handling. Assets are in `public/media/hero/`. TypeScript/lint pass and video asset serves HTTP 200. Local only, undeployed.

Visibility correction: user reported an invisible video. Browser showed active playback but a blank masked video surface; all encoded frames contained the logo. Removed CSS mask/isolation and added a poster background. The refreshed preview visibly renders the playing logo. Keep video free of CSS masking to avoid this rendering issue.

Deployment completion (2026-09-23): User approved play once on load and replay on desktop hover, then explicitly requested deployment. Scoped hero source and media committed/pushed as `8a1cc50`. Built an isolated production candidate with Webpack, passed TypeScript and production release gates (Core plus 3/3 tenants and security). Promoted only hero files, media, and build to Core; previous build and prior hero source backed up at `/var/backups/aifrogi/hero-8a1cc50`. PM2 online; live health release `8a1cc50` and status OK; homepage, both hero assets, and all three bot pages HTTP 200. Live browser confirmed video plays once, does not loop, resets to opening frame, and remains visible. TypeSafe remains off. The first-ten commercial launch itself remains a separate incomplete objective.

Homepage follow-up (2026-09-23): Fixed the hero video’s black rectangle in Chrome with screen blending, deployed as `d620e74`, and visually verified the live hero. Then moved the existing “About AiFrogi” section immediately above “Security & control” without changing either section’s content. Homepage-only change deployed as `39e8390`; production build and TypeScript passed, live HTML confirms section order, and readiness is OK. These visual changes do not establish commercial bot launch acceptance. Next launch action remains user-led use of the initial bots and targeted repair of observed issues.

Navigation follow-up (2026-09-23): At user request, removed the redundant “All AI Bots” dropdown item; the “AI Bot” parent still links to `/solutions` and individual bot links remain. Deployed as `d435c4d` after lint, production build, and TypeScript passed. Live HTML confirms the duplicate is absent and the overview link remains; readiness is OK. Next launch action remains user-led initial bot use and targeted fixes from observed issues.

Experience CTA follow-up (2026-09-23): At user request, the final “Try the bot family” button now links to the 15-day trial registration page, and the duplicate “Start your free trial” button is removed. Deployed as `0700fa8` after production build and TypeScript passed. Live Chrome final chapter confirms one CTA with the intended registration URL; production readiness is OK. Next launch action remains user-led initial bot use and targeted fixes from observed issues.

RLS/context recovery deployment (2026-09-23): after user approval, obsolete historical build/source backups and abandoned TypeSafe staging trees were removed from Core; disk use fell from 99% to 79–80% while the current rollback, recent 23 September packages, permanent release archive, database, uploads and active containers were preserved. The first promotion attempt (`d9928c0`) exposed an over-broad production `getDb()` fail-closed rule: readiness returned 500 and unwrapped bootstrap paths raised `MissingDatabaseIdentityError`. It was immediately rolled back to healthy `9d4a451`; database counts remained unchanged. The rule was narrowed to explicit protected authenticated surfaces, readiness now uses bootstrap access, and the AI-operations route gained an explicit tenant boundary. Corrected release `d9a6b58` is live. Evidence: 32 authenticated surfaces in the source guard, TypeScript, channels 245/245, production hardening 128/128, clean production Webpack build, SEC-001 passed, live/ready OK, Asavaristays embed 200, PM2 online with zero unstable restarts, TypeSafe fully off, and dashboard invariants unchanged at 5 organizations, 464 website conversations, 13 subscriptions and 2 credit transactions. Rollback remains `/var/backups/aifrogi/rls-context-e089ff6-20260923`. The unrelated Golden-bank Core promotion gate remains open for the three live tenants; no tenant intelligence was changed to bypass it.
