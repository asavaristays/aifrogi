# 2026-09-14

- Deployed `agentic-flow-20260914` to VPS `187.77.188.146` from an isolated copy of production.
- Flow Intelligence now supports custom creation, editable names, draft save, journey testing, publish/enable and disable without deleting the design.
- Added AI Bot Family recommendations and governed readiness states: Design, Govern, Assistive Live and Agentic Ready. Transactional readiness requires an enabled connector and a recovery or human-handover path.
- VPS TypeScript, 23 focused tests and the production build passed. The build generated all 87 application routes.
- Internal and public readiness returned HTTP 200 with the exact release marker. `/flow-intelligence` remains protected by a 307 login redirect and `/login` returned HTTP 200.
- Rollback backup: `/var/backups/aifrogi/agentic-flow-20260914`.
- Deployed `flow-cleanup-20260914`: removed the client-visible AI Bot Family coverage/governance banner and redundant three-step strip while retaining the underlying agentic logic and all operational flow controls.
- VPS TypeScript, 18 focused tests and the full 87-route build passed. Internal and public readiness returned the exact release; the protected Flow route retained its login redirect.
- Rollback backup: `/var/backups/aifrogi/flow-cleanup-20260914`.
- Deployed Tenant Intelligence capability `TI-016` as release `ti006-deep-crawl-final-20260914` to VPS `187.77.188.146` from a production-derived isolated stage. The release marker retains its original deployment name; the ledger ID avoids collision with the existing `TI-006` answer lifecycle.
- Recursive same-origin discovery expanded the Asavaristays verified website snapshot from 27 to 59 pages, including property, destination, package and experience routes omitted from the sitemap.
- Added tenant-scoped property/product/service extraction for headed facts, images, amenities, rooms, access, rates and policies while retaining connector authority for live availability, booking and payment.
- A Chrome production test exposed and then closed one inference defect: `Airport: Jodhpur 35 Kms` is now returned exactly rather than being expanded to an unsupported airport name. The final live response cited one approved first-party source.
- VPS TypeScript, three exact-fact tests and the complete 87-route production build passed. Public readiness returned the exact final release and all checks `ok`.
- Rollback builds retained at `/var/backups/aifrogi/ti006-deep-crawl-20260914`.
- Deployed `TI-017` as release `ti017-truth-governance-20260914`. Tenant Intelligence now exposes a source-linked Truth Review Queue for conflicts, stale evidence and commercial facts requiring confirmation; legitimate multiple website phone/email contacts are no longer treated as contradictions.
- VPS TypeScript, isolated TI-017 tests and the complete 87-route production build passed. Public readiness returned the exact release with every check `ok`; `lead-os-ai` is online with zero unstable restarts.
- Authenticated Chrome acceptance verified Asavaristays' live queue with 46 review items: 1 conflict, 0 stale facts and 45 commercial confirmations. This surfaced extraction-quality work for Bucket 2 without publishing those claims automatically.
- Rollback retained at `/var/backups/aifrogi/ti017-truth-governance-20260914`.
- Implemented Bucket 2 locally as `TI-018`: a tenant-scoped entity catalogue for properties, products and services, with generated aliases, category vocabulary, source provenance and spelling-tolerant resolution.
- Tightened website fact extraction so narrative uses of “open”, city/country names, “package” or “plan” no longer become false operating hours, addresses or prices. The Intelligence workspace now shows each recognized entity, its aliases, fact count and source.
- TypeScript and 12 focused tests passed; the complete channel and Sovereign regression suites passed. Deployment and authenticated recrawl acceptance remain pending.
- Deployed `TI-018` as release `ti018-tenant-entities-20260914`. VPS focused tests and the complete 87-route build passed; readiness returned the exact release with all checks `ok` and PM2 reported zero unstable restarts.
- Authenticated Asavaristays recrawl retained 59 pages, generated a visible catalogue of 12 source-linked properties and reduced the Truth Review Queue from 46 noisy items to 5, with no remaining false conflict badge.
- Live acceptance caught and repaired a short-token transposition edge case. The final test question `What is the nearest airport to Rohet Grah?` resolved Rohet Garh and returned the exact approved wording `Airport: Jodhpur 35 Kms` from one source, rather than inventing an airport name.
- Rollback retained at `/var/backups/aifrogi/ti018-tenant-entities-20260914`.
- Implemented Bucket 3 locally as Core capability `CI-021`. The shared runtime now makes an explicit evidence-based answer/clarify/handover decision and exits repeated unclear requests to human support rather than looping.
- Added recent-session tenant-entity continuity for natural follow-ups and retained normalized dates/quantities while preserving explicit consent requirements for phone and email.
- TypeScript, 20 focused tests, 207 channel tests and 365 Sovereign tests passed. Deployment and live multi-turn acceptance remain pending.
- Deployed `CI-021` as release `ci021-confidence-memory-20260914`. VPS TypeScript, 20 focused tests and the complete 87-route build passed; readiness returned the exact release and every health check `ok`, with zero unstable PM2 restarts.
- Live public-bot acceptance asked `What is the nearest airport to Rohet Garh?`, then `How far is it from the railway station?`. The second turn retained Rohet Garh and returned exact approved evidence: `Railway Station: Rohet 7 Kms.`
- Rollback retained at `/var/backups/aifrogi/ci021-confidence-memory-20260914`.
- Implemented Bucket 4 locally as Tenant capability `TI-019`. Every website recrawl now records an auditable page/fact change set, tenant refresh timing is visible in Intelligence, and the existing automation worker refreshes a bounded batch of due tenants.
- Repeated unanswered questions are clustered into tenant-scoped draft topics with occurrence evidence and a one-click path into the existing answer approval workflow. Neither crawl changes nor learned topics publish themselves.
- Local TypeScript, focused 3/3, channel 207/207, Sovereign 365/365 and the complete 87-route production build passed. Turbopack stalled locally without an error; the clean webpack production build completed successfully. VPS deployment and authenticated recrawl/UI acceptance remain pending explicit authorization.
- Deployed `TI-019` as release `ti019-learning-lifecycle-20260914`. The isolated VPS stage passed TypeScript, focused 3/3 and the complete 87-route build. The first promotion safely rolled back because its verifier incorrectly assumed JSON key order; the corrected order-independent gate then promoted the same tested build successfully.
- Public readiness returned the exact release with every check `ok`; PM2 is online with zero unstable restarts. Rollback retained at `/var/backups/aifrogi/ti019-learning-lifecycle-20260914`.
- Implemented Bucket 5 locally as `TI-020`. Client Owners/Admins can maintain a five-question smoke bank or ten-question golden bank, choose the expected grounded-answer or safe-handover outcome, and run it through the actual tenant answer path with fixed privacy probes.
- Certification is tenant-scoped, expires after a website recrawl or governed-answer change, and is now mandatory in both review submission and Super Admin go-live server paths. Appearance-only changes do not invalidate knowledge evidence.
- Local TypeScript, focused 7/7, channel 207/207, Sovereign 365/365 and the complete 88-route production build passed. VPS deployment remains pending explicit authorization.
- Deployed `TI-020` as release `ti020-tenant-certification-20260914`. The isolated VPS stage passed TypeScript, focused 7/7 and the complete 88-route build before promotion.
- Public readiness returned the exact release with every check `ok`; PM2 is online with zero unstable restarts. Rollback retained at `/var/backups/aifrogi/ti020-tenant-certification-20260914`.
- Implemented `CI-022` locally: a fail-closed Core release gate that runs the shared Core suite and a fresh Golden certification for every live tenant. A missing tenant property, missing Golden bank, failed expectation, privacy failure or unavailable database blocks release.
- Certification uses evaluation mode so it cannot create knowledge gaps, visitor leads or reply-credit usage; actual model tokens, attempts and latency remain tenant-attributed.
- Implemented `OPS-001` locally using the durable `UsageRecord` store. Public answers record idempotent input/output tokens, model attempts and latency; billing derives the period cost without per-answer rounding loss and displays it in Super Admin Billing and each customer record.
- Cost remains deliberately zero until the deployed model's input/output rates are configured in paise per million tokens; token quantities are retained regardless. Local TypeScript and Sovereign 370/370 passed. Deployment remains pending explicit authorization.
- Deployed `CI-022` and `OPS-001` as release `ci022-ops001-20260914`. The isolated VPS stage passed TypeScript, focused safeguards 5/5 and the complete 88-route production build. Readiness is green, the exact release marker is public, PM2 is online with zero unstable restarts, protected Billing returns its expected login redirect and the Asavaristays bot returns HTTP 200.
- The first real fleet audit passed Core 30/30 and intentionally exited blocked: Asavaristays and Webtechnosys are the two non-demo live tenants and neither yet has its required ten-question Golden bank. No later Core release is eligible until both banks pass. Synthetic showcases remain governed by the fixed bot-family regression suite.
- Rollback retained at `/var/backups/aifrogi/ci022-ops001-20260914`.
- Created and ran the two real tenant Golden banks through the authenticated Chrome Setup interface. Asavaristays passed 10/10 with property access, destination, contact, booking enquiry, verification boundary and human-handover cases. Webtechnosys passed 10/10 with services, training, pricing, filmmaking, hospitality, customer-data safeguards, office locations and human escalation cases.
- The final production `CI-022` run passed Core 30/30 and real tenant fleet coverage 2/2 with no blockers. Future knowledge changes will invalidate the relevant tenant result and require a new pass before a later Core release.
- Proved `TI-020` invalidation in production with a controlled Webtechnosys lifecycle change: one published answer was paused, Setup immediately reported the Golden pass stale, the identical answer was restored, and the tenant bank was rerun to 10/10. The final fleet gate returned Core 30/30 and live tenants 2/2.
- Activated `OPS-001` cost estimation for the actual production default `gpt-4.1-mini` model using OpenAI's current $0.40/M input and $1.60/M output rates and a dated ₹95.60/USD estimation rate: 3,824 paise/M input and 15,296 paise/M output. Token metering remains authoritative; INR cost is explicitly an estimate whose FX input must be periodically reviewed.
- Implemented `TI-021` locally: Setup can prepare a category-aware ten-question Golden-bank draft led by the tenant's approved questions and including two handover boundaries. The client must review and explicitly select **Save Golden bank**; suggestions never auto-publish or auto-certify. TypeScript, focused 5/5 tests and the complete 88-route webpack production build passed; deployment remains pending.
- Deployed `TI-021` as release `ti021-golden-assistant-20260914`. The production-derived VPS stage passed TypeScript, focused certification tests 5/5 and the complete 88-route build before promotion. Readiness is green and PM2 is online with zero unstable restarts.
- Authenticated Chrome acceptance prepared a Webtechnosys Golden draft from approved knowledge, displayed the explicit **Save Golden bank** boundary, and then cancelled it. The existing passed bank remained unchanged. The final release gate passed Core 30/30 and both live tenants 2/2 with no blockers.
- Rollback retained at `/var/backups/aifrogi/ti021-golden-assistant-20260914`.
- Implemented `OPS-002` locally to close the launch-control gaps found in the post-TI-021 audit. GitHub CI now runs the Core suite plus tenant certification, fleet-gate and usage-metering tests. The VPS release-gate wrapper obtains the live database configuration from PM2 and blocks promotion unless Core and every live tenant pass.
- Added a protected daily encrypted-backup runner and a weekly restore-drill runner. The drill restores the newest encrypted dump into a local-only disposable PostgreSQL 16 container, verifies restored public tables, and destroys the drill database/container without touching production. Shell syntax, Node syntax, TypeScript and Core regression passed locally. VPS installation and first backup/restore evidence remain pending authorization.
- Deployed `OPS-002` to VPS `187.77.188.146`. The mandatory live promotion gate passed Core 30/30 and both live tenants 2/2. Daily encrypted backup runs at 02:17 and the isolated restore drill runs Sundays at 03:43, both under non-overlapping file locks.
- Created `/var/backups/aifrogi/aifrogi-20260914T073014Z.dump.gz.enc`, verified its SHA-256 checksum, and restored 68 public tables into a disposable PostgreSQL 16 database. The drill exposed and fixed the pre-existing duplicate-public-schema failure before cron activation. The health monitor now alerts when no encrypted backup exists or the latest is older than 36 hours.
- VPS rollback retained at `/var/backups/aifrogi/ops002-launch-controls-20260914`. GitHub synchronization commit `4fac151` is prepared locally; pushing the 142-file accumulated production-source synchronization to `main` requires explicit main-branch approval.
