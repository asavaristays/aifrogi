# AiFrogi daily worklog — 2026-09-07

## Starting position

- AiFrogi is a controlled-pilot platform with public marketing, self-service and administrator onboarding, governed knowledge, eight bot personas, website delivery, human handover, measurement, governed correction, billing/support foundations and connector engineering.
- Buckets 1, 2, 3, 5A and 5B are closed within their documented scopes.
- Webtechnosys Bucket 5C private human pilot is prepared but not recorded as executed.
- Broad commercial scale, 95% real-world accuracy and blanket connector certification remain unproven.

## Decisions and discussion

- Reviewed the project documentation without changing the application or production state.
- Confirmed that current reporting must separate platform capability, connector certification and client-pilot evidence.
- Established this daily, append-only worklog system for future work.
- Founder direction: use the working day for real bot configuration and launch evidence, beginning with Webtechnosys; avoid unrelated redesign or repetitive planning.

## Implemented

- Standardised the principal `/experience` presentation actions to a compact 220px maximum width.
- Added a clearly labelled top-right close control that returns visitors to the AiFrogi homepage.
- Added Google Analytics measurement ID `G-5QQ0RTF35N` to the shared application root so it loads once across all AiFrogi routes, using Next.js after-interaction script loading.

## Verification

- VPS production build completed successfully before promotion.
- Production `/experience` returned HTTP 200.
- Browser accessibility inspection confirmed `Close presentation`, `Watch 45 seconds` and the presentation chapter controls.
- These checks establish the deployed interface behaviour; they are not customer usability or accessibility certification.
- Production readiness at `2026-09-07T02:37:35.548Z`: healthy release `experience-controls-20260907`; database, session secret, public URL and configured webhook checks passed.
- Read-only Webtechnosys audit confirmed the website bot is `LIVE`, installation is detected, the trial is active until 16 September 2026, nine claims are published, freshness is 100%, and conflicts, unsigned claims, pending previews and incorrect-answer flags are zero.
- Live browser verification passed: a training enquiry returned the approved training-booking link; the follow-up `yes` returned that link and explicitly stated that no booking or payment occurred; a natural-language phone request returned `+91-7410582898`.
- Formal readiness audit after reconciliation: nine published claims, generic coverage 30%, freshness 100%, zero open gaps, 44 answer-evidence records, zero decision inconsistencies, two retained historical negative votes and two pending replay reviews.
- Google Analytics verification: TypeScript passed, focused installation test 1/1 passed and the production webpack build passed. After deployment, the measurement ID was present on the public homepage, pricing page and application login page.

## Deployed

- Release: `experience-controls-20260907`.
- Production page: `https://aifrogi.com/experience`.
- Rollback backup: `/var/backups/aifrogi/experience-controls-20260907.SxRcqv`.
- Google Analytics release: `google-analytics-20260907`; rollback backup: `/var/backups/aifrogi/google-analytics-20260907.fQPJOD`.

## Issues and corrections

- Historical documentation contains conflicting states for trial length and Razorpay maturity. The latest canonical evidence must take priority; older records remain history.
- No production data, tenant knowledge, billing state or connector configuration was changed during the documentation recap.
- Reconciled three stale Webtechnosys gap records with an audited production transaction: phone and training questions now reference their existing published claims; HotelRadar was dismissed as owner-confirmed out of scope. Independent read-back confirmed zero open knowledge gaps. No published answer was changed.
- The reconciliation helper committed successfully but encountered the known Node/Prisma shutdown `SIGABRT`; an independent read-only audit confirmed the database result. This runner-lifecycle issue remains separate from the successful business transaction.

## Open items

- Execute and review Webtechnosys Bucket 5C with the defined three testers and 24-conversation bank.
- Complete one retained real billing lifecycle: payment, verification, reminder, grace, suspension and restoration.
- Complete one retained real support lifecycle from customer ticket through email exchange and resolution.
- Formally reconcile and close the remaining Google connector authority, recovery and revocation evidence.
- Reassess readiness only after real-client evidence is available.
- Webtechnosys generic readiness coverage remains 30%; seven business topics still require client-owner truth/approval. The bot being technically `LIVE` does not close the private-pilot knowledge gate.
- The two historical negative votes must remain preserved and receive reviewer disposition based on the corrected live replay; they must not be deleted or converted into positive votes.

## End-of-day position

- Daily project tracking is active.
- Future completed work should be appended to this file today; a new dated file should be created on the next working day.

## Continued direction — widget upgrade

- Founder direction: the website widget upgrade is the main task for the remainder of 7 September 2026.
- Keep implementation, verification, deployment evidence, issues and open items tracked in this daily record as the work progresses.
- Hold scope to the widget and its directly shared embed/standalone runtime; do not drift into unrelated product work or repeat planning loops.
- No widget implementation or production change is claimed by this direction entry.

## Widget prerequisite analysis — capacity and scope

- A permission-correct local production build passed in 14.26 seconds: compilation 4.8 seconds, TypeScript 7.2 seconds and static generation 0.22 seconds. The earlier 370.64-second failure was caused by the restricted diagnostic environment preventing a Turbopack helper from binding a local port; it was not a production backend failure.
- The core test suite passed all 468 tests in 2.12 seconds. This is automated local evidence, not live production acceptance.
- The development machine has approximately 13 GB free at 94% disk use; the local project contains approximately 915 MB of dependencies and 576 MB of regenerable `.next` output.
- Read-only VPS inspection found 16 GB free at 84% disk use. `/var/backups/aifrogi` contains 53 backup directories using 9.6 GB. `/var/www` uses 32 GB, including four 1.7 GB legacy `lead-os-ai-backup-*` trees, a 3.8 GB old build tree, a 3.8 GB video staging tree and a 1.1 GB hardening staging tree.
- System cache is only 149 MB and system logs are 288 MB; old application backups and staging trees are therefore the primary capacity opportunity.
- The current application exposes 83 API routes: 11 WhatsApp routes, 6 appointment routes, 5 FlowCart routes, 3 pilot-measurement routes, 4 website-bot routes and 54 other routes.
- WhatsApp is still coupled across public pages, authenticated navigation, onboarding, campaigns, Prisma models, shared services and tests. Removing its 11 routes alone would leave broken product paths; separation requires an explicit archive/subdomain boundary and regression-tested migration.
- No backup, cache, API route, WhatsApp code or production file was removed during this analysis.

## Widget prerequisite cleanup — completed

### Implemented

- Removed 50 superseded AiFrogi VPS backup directories while preserving the stable `releases` archive and the two latest rollback points: `experience-button-20260907.8l2mSt` and `experience-controls-20260907.SxRcqv`.
- Removed four obsolete August `lead-os-ai-backup-*` application copies and the obsolete build, video and hardening staging trees from `/var/www`.
- Removed old root-level source `.tar.gz` and `.tgz` archives from the AiFrogi backup folder. Database dumps, encrypted database backups, checksums and configuration snapshots were retained.
- Removed six obsolete production `.next-before-*` / `.next-mixed-*` build caches. The active production `.next` was retained.
- Removed the regenerable local development `.next` cache.

### Verification

- VPS disk use reduced from 81 GB/84% to 56 GB/58%; free space increased from 16 GB to 42 GB.
- `/var/backups/aifrogi` reduced from 9.6 GB to 618 MB and contains the two retained rollback directories plus the stable `releases` archive.
- Production `/var/www/lead-os-ai` remains present with its active `.next` and now uses approximately 1.2 GB.
- Twenty-four database dump, encrypted-backup or checksum files remain in the backup root.
- Live readiness remained healthy on release `experience-controls-20260907`; database, session secret, public URL and webhook configuration checks returned `ok`.

### Remaining boundary

- No API route, WhatsApp code, database schema or product navigation was removed in this cleanup. WhatsApp separation and API reduction require a separately verified code change.

## Website-only product boundary — implemented locally

### Implemented

- Retired 15 main-application API routes: 11 WhatsApp integration/public routes, two Meta onboarding routes and two campaign routes. The application API surface reduced from 83 to 68 routes.
- Removed the dedicated WhatsApp API, client WhatsApp workspace, WhatsApp settings, Super Admin WhatsApp operations and campaigns pages.
- Removed stale product-tour, preview and FlowCart marketing pages that continued to advertise the deferred messaging product.
- Removed WhatsApp, Meta and campaign references from public navigation, pricing, sitemap, status, registration, legal pages and reachable marketing content. Help articles containing deferred-product material are excluded from listing, static generation and direct article access.
- Forced both client and Super Admin bot-profile saves to the `WEBSITE` channel, so a crafted payload cannot re-enable the deferred channel.
- Removed the obsolete WhatsApp configuration-save action from the shared Super Admin customer API.
- Changed Team Inbox to load website conversations without querying the WhatsApp integration backend.
- Kept lower-level legacy adapters, database fields and historical tests where they remain useful for a future separate-subdomain extraction; they are not exposed as current main-product routes.

### Verification

- `npm run typecheck` passed.
- The updated core suite passed all 467 tests in 1.95 seconds with zero failures.
- The production build passed in 12.9 seconds: compile 4.6 seconds, TypeScript 6.1 seconds and 71 static pages generated in 183 ms.
- The resulting route manifest contains no WhatsApp, Meta onboarding or campaign page/API route.

### Deployment boundary

- These product-boundary changes are local and have not been deployed. Production remains on healthy release `experience-controls-20260907` until an explicit deployment decision.

## Widget upgrade — responsive foundation completed locally

### Implemented

- Replaced the wide launcher pill with a compact 58px circular button fixed to the right-bottom safe area. It displays the configured business logo and falls back to an `AI` mark.
- Reduced the desktop open panel from the previous 410x720 maximum to 376x640, while retaining viewport-safe maximum dimensions.
- Added a dedicated mobile breakpoint at 520px: the open widget uses the full dynamic viewport, removes rounded corners and respects top, bottom and side safe areas.
- Removed the embedded chat's 520px minimum-height constraint so short landscape screens can use the available height without overflow.
- Tightened header, avatar, transcript and composer spacing; added a short-height desktop mode for screens under 620px high.
- Connected launcher appearance to the existing governed backend sources: bot name from `BotProfile`; logo, colour and welcome message from knowledge appearance settings. No duplicate configuration store was introduced.
- Applied configured bot name, logo and colour to the Webtechnosys runtime instead of hard-coded identity values.
- The presentation and `/experience` implementation were not changed.

### Verification

- Focused widget lifecycle tests passed: 5/5.
- Full core suite passed: 467/467 in 2.15 seconds.
- TypeScript validation passed.
- Production build passed in 12.39 seconds: compile 4.5 seconds, TypeScript 6.0 seconds and static generation 203 ms.
- Local visual preview could start, but the local database had no servable `webtechnosys` profile, so automated source, lifecycle and build gates are the current evidence. Live responsive visual acceptance remains a pre-deployment requirement.

### Deployment boundary

- The responsive widget upgrade is local only and has not been deployed.

## Widget responsive acceptance — completed locally

- Added a development-only responsive QA path through the real shared embed component. It cannot bypass lifecycle checks in production because the fallback is gated by `NODE_ENV === "development"`.
- Visual inspection found that the generic website-bot header lacked the minimize control available in the Webtechnosys variant. Added the missing control and made the generic header compact, logo-aware and safe for long bot names.
- Browser viewport checks passed at 360x640, 390x844, 768x1024 and 1366x768 with no horizontal or vertical document overflow.
- The actual compact desktop panel size, 376x640, kept the header, composer, input, send and minimize controls fully inside its bounds.
- A 360x400 reduced-height check simulating an open mobile keyboard retained a usable transcript area and kept the composer inside the visible viewport.
- Final focused lifecycle tests passed 5/5, TypeScript passed and the final production build passed in 12.50 seconds.
- The `/experience` presentation remained untouched.

## Widget responsive release — deployed

- Production release: `widget-responsive-20260907`.
- Rollback backup: `/var/backups/aifrogi/widget-responsive-20260907.mrA941`.
- Built an isolated VPS stage from the current production baseline plus only the four verified widget files; unrelated local worktree changes were not included.
- VPS staging build passed before promotion. Production promotion completed through the health-gated wrapper with automatic rollback protection.
- Post-deployment readiness returned `ok` for the database, session secret, public URL, Meta webhook signature and legacy inbound token.
- Live standalone bot, launcher embed and installer returned HTTP 200. Installer smoke verified the compact launcher, full dynamic-viewport mobile rule and trusted minimize message.
- The deployed installer source checksum exactly matched the tested stage.
- Public `/experience` remained HTTP 200 and was not changed by this release.
- Removed the completed VPS staging tree and uploaded archive after verification; retained the rollback backup. VPS remains at 58% disk use with approximately 42 GB free.
- The earlier website-only product/API reduction remains local and was deliberately excluded from this widget-only production release.

## Widget initial menu stability — deployed

- Production release: `widget-menu-stability-20260907`.
- Rollback backup: `/var/backups/aifrogi/widget-menu-stability-20260907.z3NHUL`.
- Corrected only the initial menu layout shift shown in the supplied screenshots: menu rows now size to their content from the first render and cannot stretch one card across the available panel height.
- The change is limited to `components/website-bot/webtechnosys-navigation.module.css`; no backend, API route, widget presentation content or `/experience` change was included.
- Six immediate browser reload checks at 390x680 produced stable first-frame menu heights of 60px for all five visible choices on every run.
- TypeScript validation passed and the focused widget lifecycle suite passed 5/5 before deployment.
- The isolated VPS build passed, including TypeScript and all 98 static pages. The production health gate and standalone, embed and installer smoke checks all passed.
- Live readiness reports release `widget-menu-stability-20260907`; the embed and `/experience` return HTTP 200. The deployed stylesheet checksum matches the tested source exactly.
- Removed only the temporary VPS stage and uploaded archive after verification; retained the rollback backup.

## Bot appearance ownership — implemented locally

- Moved the canonical bot appearance editor from Intelligence to `Setup → Bot appearance`. Intelligence now shows only a quick-trial shortcut to the Setup section, avoiding duplicate configuration forms.
- Added bot name, brand colour, welcome message and optional logo URL controls with a live preview of the widget header, welcome bubble and 58px launcher.
- Added explicit logo guidance: square 512 × 512 px PNG or WebP, maximum 1 MB; displayed at 30–36 px, so small text should be avoided.
- Added client-side public HTTPS logo validation and automatic readable light/dark launcher text preview for the selected brand colour.
- Kept existing governed save paths: bot name uses the bot-profile endpoint; colour, welcome message and logo use the existing knowledge-settings endpoint. No new API route or duplicate settings store was added.
- Owner/Admin editing restrictions remain enforced in both the interface and existing server endpoints.
- TypeScript passed, seven focused navigation/widget tests passed, and the production build passed in 13.3 seconds (5.0-second compile, 6.3-second TypeScript, 71 static pages in 208 ms).
- Local visual access correctly remained protected by authentication.

## Bot appearance ownership — deployed and Chrome-verified

- Production release: `bot-appearance-20260907`.
- Rollback backup: `/var/backups/aifrogi/bot-appearance-20260907.ee9Om9`.
- Compared the local change directly with the current production source before packaging; only the Setup editor, Intelligence shortcut and deployment wrapper were promoted.
- Isolated VPS build passed with TypeScript and all 98 production static pages before the live switch.
- Verified in the existing authenticated Chrome session that `Setup → Bot appearance` is visible at the top of `/setup`, with bot name, brand colour, welcome message, logo guidance, save control and live preview.
- Verified in Chrome that the quick-trial Intelligence page shows the `Personalise your bot in Setup` shortcut and that it returns to `/setup#bot-appearance`.
- Production readiness is healthy on release `bot-appearance-20260907`; the deployed editor checksum matches the tested stage.
- Removed the temporary VPS stage and archive while retaining the rollback backup.

## Self-service checklist evidence — implemented locally, deployment pending

- Replaced the hard-coded incomplete states for `Test customer questions` and `Install on your website` with automatic evidence checks.
- Installation completion now reads the existing governed lifecycle evidence (`installationDetectedAt`, with `LIVE` as a valid completed state).
- Test completion now reads either an authenticated `WEBSITE_BOT_TEST_COMPLETED` activity or existing website-bot answer evidence, preserving prior real tests and conversations.
- Added authenticated `/api/knowledge/test`; every successful Intelligence test records completion automatically. The Intelligence tester now uses this website-only route instead of the deferred channel route.
- Removed the website knowledge-answer service's dependency on deferred-channel menu configuration and replaced it with website-bot-specific capabilities.
- Added two automation regression tests. TypeScript and all nine focused tests passed; the production build passed in 13.8 seconds with 72 static pages.
- Production upload to VPS `187.77.188.146` was blocked pending explicit authorization for that destination. No production state changed in this step.

## Self-service checklist evidence — deployed and Webtechnosys verified

- Explicit authorization received to upload and deploy the checklist automation package to VPS `187.77.188.146`.
- Production release: `setup-evidence-automation-20260907`.
- Rollback backup: `/var/backups/aifrogi/setup-evidence-automation-20260907.Y89Rbu`.
- Isolated VPS build passed with TypeScript and 99 production pages. The authenticated test route rejects unauthenticated requests with HTTP 401.
- After promotion, the existing authenticated Webtechnosys Chrome workspace automatically resolved all four checklist steps as complete.
- Chrome showed green ticks for appearance, business knowledge, customer-question testing and website installation. Test copy confirms recorded website-bot evidence; installation copy confirms a valid widget load was detected.
- No manual database status override was required: the pre-existing Webtechnosys test and installation evidence was sufficient.
- Production health is green on release `setup-evidence-automation-20260907`; the deployed Setup source checksum matches the tested stage.
- Removed the temporary VPS stage and uploaded archive while retaining the rollback backup.

## iPhone XR widget diagnosis and host-loader fix — implemented locally

- Reproduced the supplied iPhone XR issue in the live Webtechnosys Chrome tab at 414×896: the open panel measured 390×680, began off-screen and left the launcher visible behind it.
- Confirmed the AiFrogi governed installer was not responsible. Webtechnosys loads a legacy host asset, `/assets/aifrogi-loader-closed-20260907.js`, with hard-coded desktop sizing, `100vh`, a non-launcher embed URL and no launcher hiding.
- Added a focused host-loader correction: mobile panel uses fixed `inset: 0`, `100vw × 100dvh` with `100vh` fallback, launcher mode, trusted close messaging, hidden launcher while open, background scroll lock and orientation-safe resize handling.
- Desktop placement and launcher presentation remain unchanged.
- The current legacy loader is served through Cloudflare with `max-age=2592000, immutable`; the release therefore uses a new responsive asset filename and updates the homepage reference to avoid a 30-day stale mobile cache.
- JavaScript syntax and six responsive invariants passed in the local verification script.
- The Webtechnosys host-loader package is not yet deployed; live Chrome still reflects the reproduced legacy behavior.

## Persona intelligence and persistent embed options — implemented locally

- Added a focused `Setup → Persona intelligence` editor for bot purpose, tone, languages, prohibited claims, human-escalation topics and an optional approved safe fallback.
- Kept factual business information and answer approval in Intelligence; persona settings control behaviour and safety without duplicating knowledge.
- Expanded the self-service checklist to five evidence-based steps by adding bot behaviour between appearance and business knowledge.
- Embedded the tenant-specific website installation panel directly in Setup. JavaScript, iframe, WordPress and standalone options remain visible after installation and retain their copy controls.
- Changed the completed installation action to `View embed options`; it anchors to the persistent client installation section instead of the public marketing guide.
- Reused the existing bot-profile and installation lifecycle APIs. No new route or duplicate installation state was added.
- TypeScript and nine focused tests passed. The production build passed in 14.1 seconds with 72 static pages.
- This new source package is verified locally and has not yet been uploaded or deployed.

## Persona intelligence and persistent embed options — deployed

- Explicit authorization received to upload and deploy this source package to VPS `187.77.188.146`.
- Production release: `persona-embed-options-20260907`.
- Rollback backup: `/var/backups/aifrogi/persona-embed-options-20260907.XhBy7c`.
- The isolated VPS build passed with TypeScript and all 99 production pages before promotion.
- Verified in the authenticated Webtechnosys Chrome workspace that `Setup → Persona intelligence` displays bot purpose, tone, languages, prohibited claims, human-escalation topics and approved fallback controls.
- Verified that the LIVE Webtechnosys installation panel remains available after completion with JavaScript, iframe, WordPress and standalone options and copy controls.
- The installation panel shows completed states for code generated, installed on website, platform approval and AI Bot live.
- Production health is green on release `persona-embed-options-20260907`; the deployed persona editor checksum matches the tested stage.
- Removed the temporary VPS stage and uploaded archive while retaining the rollback backup.

## Website-bot-only Setup — deployed and Chrome-verified

- Production release: `website-bot-setup-20260907`.
- Rollback backup: `/var/backups/aifrogi/website-bot-setup-20260907.yUVo3P`.
- Removed the legacy operating-flow content from the client Setup page, including WhatsApp connection, Meta dependency, campaign, business-verification and billing-readiness milestones.
- Setup now contains only four website-bot steps: choose appearance, add approved business knowledge, test customer questions and install on the website.
- Removed the Setup page dependency on the legacy product-flow loader; no backend or API route was added.
- A source-level scope check confirmed no WhatsApp, Meta, campaign, PAN/GST or business-verification reference remains in the Setup implementation.
- TypeScript, seven focused navigation/widget tests and the local production build passed. The isolated VPS build also passed before promotion.
- Verified in the existing authenticated Chrome session that `AI Bot setup` and `Four steps to prepare your bot` are visible, and that none of the excluded references appears in the rendered page.
- Production health is green on release `website-bot-setup-20260907`; the deployed Setup source checksum matches the tested stage.
- Removed the temporary VPS stage and archive while retaining the rollback backup.

## Generic widget viewport responsiveness — implemented locally

- Reworked the responsive rule around available viewport constraints rather than named devices or model-specific dimensions.
- Compact mode activates when either width is at most 640px or height is at most 520px, covering phones in portrait/landscape and short browser windows.
- The open widget follows `window.visualViewport` width, height and offsets so mobile browser chrome, zoom, orientation and on-screen keyboard changes do not leave the panel clipped or off-screen.
- The launcher is hidden while the panel is open and restored by the trusted launcher-mode minimize message.
- Applied the same behavior to both the official AiFrogi website-bot installer and the Webtechnosys legacy host loader package.
- The Webtechnosys promotion uses a new asset filename to bypass the existing 30-day immutable cache.
- Loader syntax and responsive invariants passed; five focused lifecycle/installer tests, TypeScript validation and the full 72-page production build passed.
- This responsive package is not yet uploaded or deployed; production remains unchanged pending explicit authorization for both affected sites.

## Webtechnosys generic responsive loader — deployed

- Explicit authorization received for the Webtechnosys widget-loader package on VPS `187.77.188.146`; the separate AiFrogi installer source remains undeployed.
- Production release: `widget-viewport-responsive-20260907`.
- Rollback backup: `/var/backups/webtechnosys/widget-responsive-20260907.9RZ3UB`.
- Published `/assets/aifrogi-loader-responsive-20260907.js` and changed the homepage to reference that new filename, bypassing the legacy loader's 30-day immutable cache.
- Public HTTP verification returned 200 for the new loader and confirmed the homepage uses it.
- Live Chrome verified the new asset, launcher-mode embed, constrained desktop panel and hidden launcher while the widget is open.
- The first promotion stopped before changes because the backup parent did not exist. A second validation mismatch also restored the index automatically; after correcting only the whitespace-sensitive release assertion, promotion passed.
- Removed the exact temporary VPS staging directory after final production verification; retained the rollback backup.

## Widget minimize control — fixed and deployed

- Root cause: the cross-origin AiFrogi embed targeted its own origin when posting the close message to the Webtechnosys parent, so the browser rejected delivery.
- Changed the non-sensitive close signal to reach the parent window; the Webtechnosys loader still validates the sender origin and bot slug before closing.
- Added a regression assertion for the cross-origin close signal. Five focused tests, TypeScript validation and the local 72-page production build passed.
- The isolated VPS production build passed with all 99 pages before promotion.
- Production release: `widget-close-fix-20260907`.
- Rollback backup: `/var/backups/aifrogi/widget-close-fix-20260907.sZmY9a`.
- Live Chrome end-to-end verification passed after refresh: clicking `Minimize AI Bot` hid the panel, restored the launcher and reset `aria-expanded` to false.
- Production health is green and the exact temporary VPS staging directory was removed; rollback retained.

## Deployment batching rule — adopted

- Added the durable project rule in `docs/vps-change-strategy.md` to reduce repeated builds, uploads, restarts, verification cycles and context use.
- Default: group up to three closely related changes within one product area, then perform one full validation and one VPS release.
- Exception: immediately deploy an isolated production-critical hotfix such as a broken open, close, send or installation action.
- Every deployment remains production-based, file-scoped, explicitly authorized, automatically reversible and browser-verified.
- Current queue: the official generic AiFrogi installer responsiveness change is locally verified but undeployed; it will be the first item in the next Widget batch.

## Pending VPS ledger — adopted

- Added `docs/PENDING_VPS_CHANGES.md` as the single source of truth for locally completed but undeployed changes.
- Entries are added only after local verification and removed only after VPS deployment, health checks and client-facing verification pass.
- When no deployment is pending, the ledger remains present with no change entries so an empty file body clearly means production is synchronized.
- Permanent deployment results remain in the dated daily log; the pending ledger stays short and operational.

## Reusable website-bot main menu — implemented locally

- Added `Setup → Main menu` so workspace owners/admins can enable the menu, edit its heading, create, remove and reorder options, select approved icons and actions, feature one option and preview the result.
- Supported safe actions: public HTTPS link, start chat, phone call, email and one submenu level. Limits are six main options and eight submenu options.
- Added authenticated `/api/setup/menu`; menu data remains property-scoped alongside the existing widget appearance settings and does not enter Intelligence.
- Replaced the tenant-only navigation behavior with a reusable renderer used by both embedded and standalone website bots.
- Existing Webtechnosys installations automatically receive their familiar five-option menu without requiring manual re-entry. Other tenants start with the menu disabled.
- Website URLs, phone numbers, email addresses, nesting depth and option counts are normalized server-side; custom HTML and JavaScript are not accepted.
- Nine focused menu/widget tests, TypeScript validation and the final 73-page production build passed.
- Added this as item two in the pending Widget VPS batch. It is not deployed yet.

## Widget Dark, Light and System themes — implemented locally

- Added `Widget theme` to `Setup → Bot appearance` with Dark, Light and System choices and an immediate themed preview.
- Dark is the backward-compatible default for existing clients. System follows each visitor's `prefers-color-scheme` setting without client-side polling.
- Persisted the selection in property-scoped appearance settings and exposed it through the existing appearance/install response without adding a new API route.
- Passed the setting to both embedded and standalone bots and themed the header, menu, messages, composer, controls, borders and muted text through shared semantic colour variables.
- Brand colour and automatic readable accent text remain independent of surface theme.
- Ten focused widget/menu tests, TypeScript validation, diff checks and the final 73-page production build passed.
- Added as item one in the next pending Widget VPS batch; it is not deployed yet.

## Widget configuration batch — deployed

- Explicit approval received to deploy the two queued Widget changes together.
- Production release: `widget-config-batch-20260907`.
- Rollback backup: `/var/backups/aifrogi/widget-config-batch-20260907.y8dDOz`.
- Deployed the generic visual-viewport installer responsiveness and reusable Setup main-menu system in one build, backup, restart and verification cycle.
- The first isolated VPS build exposed a production-only preview fixture that constructed the older settings shape. The menu field was made backward-compatible; production was unchanged during this failed stage build.
- The corrected isolated VPS build passed TypeScript and all 100 production pages.
- Post-promotion routes passed: Setup redirected to authentication as expected, Webtechnosys embed returned 200, unauthenticated menu API returned 401 and public installer JavaScript returned 200.
- Authenticated Chrome verified `Setup → Main menu`, all migrated Webtechnosys options, safe action controls, ordering, featured selection and live preview.
- Live Webtechnosys Chrome verified the reusable menu heading, five main options, Chat tab, submenu entry and trusted minimize control.
- Production health is green; the public installer contains the visual-viewport behavior. Removed the exact VPS stage and local temporary archive; retained rollback.
- Cleared all entries from `docs/PENDING_VPS_CHANGES.md` because local and VPS are synchronized for this batch.

## Today / Operate website-bot readiness — implemented locally

- Removed business verification, KYC and onboarding from the Today readiness calculation and client-facing account health.
- Replaced the legacy channel gate with evidence-based website-bot readiness: lifecycle, appearance, persona intelligence, approved knowledge, safety/escalation, customer-question testing and detected installation.
- Action authority and verified connectors are required only when the customer enables an action-capable operating mode.
- Added Plan and Usage matrix health to Today; the matrix reports hard-stop protection or explicitly approved overage instead of silently allowing excess usage.
- Corrected live website bots that were shown as `Setup`, and routed every Today conversation link to Team Inbox rather than the removed WhatsApp workspace.
- Confirmed the Super Admin Message Matrix and policy endpoint already exist and are covered by focused tests.
- TypeScript, 11 focused readiness/matrix/automation tests, diff hygiene and the complete 73-page Webpack production build passed. The default Turbopack build stalled silently during compilation and was stopped; the alternate compiler confirmed there is no application build defect.
- Added the locally verified change to the separate pending Operate VPS batch; production is unchanged until explicit deployment authorization.

## Today / Operate website-bot readiness — deployed

- Explicit approval received for the Today/Operate deployment.
- Production release: `today-operate-20260907`; previous release: `widget-config-batch-20260907`.
- Rollback backup: `/var/backups/aifrogi/today-operate-20260907.3GQUnY`.
- Deployed exactly the Today data logic, Today UI, readiness evaluator and regression test; the pending widget-theme change was not included.
- The production-based isolated stage passed TypeScript, nine relevant tests and the complete 100-route Webpack production build before promotion.
- Production health, Dashboard, Setup and Message Matrix route checks passed. Local and deployed checksums match for all three runtime source files.
- Authenticated Chrome confirmed the Webtechnosys AI Agency bot is LIVE and 100% ready with all nine evidence-based checks green. Account health displays `Ready to operate`, `15-Day Trial`, `32 pages` and `Hard-stop protected`; business verification is absent and inbox actions resolve to Team Inbox.
- Removed the completed Operate entry from the pending VPS ledger and deleted the exact temporary VPS stage. The rollback backup remains available.

## Widget Dark, Light and System themes — deployed

- Explicit approval received for the pending widget-theme package.
- Production release: `widget-theme-20260907`; previous release: `today-operate-20260907`.
- Rollback backup: `/var/backups/aifrogi/widget-theme-20260907.HZSDbe`.
- Compared every runtime file directly with current production before staging; the package contained only the theme-selection differences on top of the already deployed responsive widget, main menu and Today/Operate releases.
- Deployed Dark, Light and System choices in Setup, persisted the selection in property-scoped settings, exposed it through the installer metadata and passed it to embedded and standalone bots.
- System mode follows `prefers-color-scheme`; Dark remains the backward-compatible default. Shared semantic variables theme the shell, navigation, messages, composer, controls and muted text while preserving the business brand colour.
- The production-based isolated stage passed TypeScript, seven widget lifecycle/theme tests and the complete 100-route Webpack build.
- Production health, Setup, Webtechnosys embed and public installer checks passed. Authenticated Chrome confirmed the three theme controls and live Dark preview are visible in `Setup · bot appearance`.
- Removed the exact temporary local comparison directory and VPS stage. The rollback backup remains available, and the pending VPS ledger is empty.

## Prepaid and free AI reply credits — implemented locally

- Added an immutable organization credit ledger covering paid purchases and Super Admin free grants without changing the subscription plan.
- Added prepaid packs: 500 credits for ₹399, 1,500 for ₹999 and 5,000 for ₹2,999. Paid packs are valid for 90 days and are activated only after Razorpay signature, order, workspace, pack, amount, currency and captured-payment verification.
- Paid activation is idempotent by Razorpay payment reference and creates both a paid invoice and platform audit entry.
- Added a Billing checkout and balance view showing granted, used-beyond-plan and remaining credits. Included plan replies are consumed first; active purchased/free credits extend the same hard entitlement limit afterward.
- Added Super Admin free-credit grants to Message Matrix with a mandatory business reason, optional future expiry, quantity validation and immutable audit evidence.
- Updated public pricing to disclose packs, validity, applicable-tax boundary, safe hard stop and the absence of automatic top-up or silent overage.
- Split the browser-safe credit catalogue from the server-only Prisma ledger after the production build identified the initial module-boundary defect.
- Prisma generation, TypeScript, eight focused billing/pricing tests, scoped diff hygiene and the complete 75-route Webpack production build passed.
- Added the database-bearing Billing package to `docs/PENDING_VPS_CHANGES.md`. Production remains unchanged pending explicit deployment authorization.

## Governed free-credit recommendations — implemented locally

- Split the client balance into purchased and promotional credits while retaining one transparent total and consuming included plan allowance first.
- Added deterministic retention suggestions: 100 credits when trial or paid usage reaches 80%, and 250 for paid accounts at 95%; suggested grants expire after 30 days.
- Suppressed suggestions when more than 50 extra credits remain, usage is below 80%, or any free grant was issued in the previous 30 days.
- Suggestions prefill the amount, reason and expiry for review but never write to the ledger. Only Super Admin can explicitly submit the audited grant.
- Client Billing now presents purchased and promotional remaining balances separately; promotional credits are applied before purchased credits after the included plan allowance.
- Added an Admin → Today AI credit snapshot using the shared Message Matrix source: current-period AI replies used, purchased credits remaining, promotional credits remaining and grant suggestions awaiting review.

## Billing credits and Admin Today — deployed

- Deployed release `billing-credits-20260907` to AiFrogi VPS `187.77.188.146` after explicit authorization.
- Took and verified PostgreSQL backup `/var/backups/aifrogi/billing-credits-20260907.4wDyUf/database.dump`; application rollback is stored in the same directory.
- Applied additive migration `20260907083000_ai_reply_credit_ledger` successfully and promoted with one PM2 restart.
- Production health passed with the new release. `/admin`, `/admin/message-matrix` and `/billing` returned the expected authenticated redirects; `/pricing` returned 200.
- Chrome verified live public ₹399/₹999/₹2,999 packs and the Client Billing credit panel with separate purchased/promotional balances, used credits, safe-stop disclosure and purchase control.
- Removed the stale `/whatsapp-api` page discovered by the production-based build instead of restoring the retired WhatsApp pricing component.
- Cleared the Billing batch from `docs/PENDING_VPS_CHANGES.md` after promotion and verification.

## Billing allocation history — implemented locally

- Confirmed paid pack activation is already atomic with its invoice and audit record, and Super Admin free-credit allocation is atomic with its audit record; both interfaces refresh after success.
- Clarified that an active trial or paid plan can purchase prepaid credits. A credit pack does not replace the monthly plan after the 15-day trial expires.
- Added one shared tabular report to Client Billing and Super Admin customer Billing showing current-period AI usage, included allowance, extra usage/remaining balance, paid packs, payment amount/reference, free grants, allocation reason and expiry.
- Added explicit `allocatedImmediately` success confirmation to both paid-verification and Super Admin grant APIs.

## Billing history and checkout handling — deployed

- Deployed release `billing-history-20260907` to AiFrogi VPS `187.77.188.146`; rollback is `/var/backups/aifrogi/billing-history-20260907.7Mm0wW`.
- Production health passed. Protected Client/Super Admin Billing routes returned expected redirects and the unauthenticated credit-order endpoint returned JSON rather than HTML.
- Chrome verified the live Client Billing history table, including current-period usage, included allowance, extra usage and remaining balance; future paid packs and governed grants use the same table.
- Replaced the raw `Unexpected token '<'` browser failure with a safe payment-service error when an upstream response is not JSON.
- Razorpay configuration names exist in the protected VPS environment, but Razorpay’s read-only API authentication check returned HTTP 401. The stored Key ID/Secret pair must be replaced with a valid matching pair before live order creation can be enabled and verified.
- Cleared the deployed Billing history batch from `docs/PENDING_VPS_CHANGES.md`.

## Full billing reconciliation — implemented locally

- Rechecked client plan activation and prepaid-credit payment points from checkout order through Razorpay signature/order/payment validation, atomic activation, invoice/credit creation, audit evidence and immediate UI refresh.
- Added content-type validation to plan checkout so upstream HTML can never surface as a raw JSON parse exception.
- Removed recent-entry caps from customer invoices and credit transactions so Client and Super Admin customer Billing retain the complete financial history.
- Added a complete Credit transactions register to Super Admin Billing, covering customer, purchase/free-grant type, quantity, paid value, payment reference or grant reason and expiry.
- Added explicit immediate activation confirmation to the verified plan-payment API.

## Full billing reconciliation — deployed

- Deployed release `billing-reconciliation-20260907` to AiFrogi VPS `187.77.188.146`; rollback is `/var/backups/aifrogi/billing-reconciliation-20260907.EfMfX1`.
- Reconfirmed both frontend payment entry points: monthly/yearly plan activation and ₹399/₹999/₹2,999 prepaid credit packs now reject non-JSON upstream responses safely.
- Reconfirmed backend verification: authenticated workspace admin, approved plan/pack, Razorpay signature, order ownership, payment/order match, exact amount/currency and captured-payment state are required before activation.
- Plan activation, paid-credit allocation, invoice creation and audit evidence remain transactionally linked and idempotent; failed or unmatched payments allocate nothing.
- Client Billing retains complete invoices and complete credit allocation history. Super Admin Billing retains complete invoices and now includes the cross-customer Credit transactions register.
- Production health passed; Client Billing, Super Admin Billing and both protected order endpoints returned expected responses. The order APIs returned JSON at the authentication boundary.
- Live Razorpay order creation remains blocked because the stored VPS credential pair returns HTTP 401 from Razorpay. No charge was attempted.
- Cleared the deployed reconciliation batch from `docs/PENDING_VPS_CHANGES.md`.

## Razorpay live keys — corrected and verified

- Used `/Users/manishpurohit/Desktop/rzp-key-2.csv` after confirming the Desktop and Downloads copies were byte-for-byte identical. The original local files were left untouched and credential values were never printed.
- The CSV contained exactly one correctly shaped live key pair. Replaced both VPS values together and retained protected rollback backup `/var/www/lead-os-ai/.env.local.bak-razorpay-20260907T093442Z`.
- Razorpay accepted the replacement pair and created one unpaid live-mode ₹399 diagnostic order. No payment was captured and no AiFrogi credit or invoice was created from this configuration check.
- Restarted AiFrogi once with the corrected credentials. Production health remained green on release `billing-reconciliation-20260907`.

## Billing email notifications — implemented locally

- Reused the configured SMTP sender and approved AiFrogi black/gold email structure for plan activation, paid-credit allocation and Super Admin free-credit grants.
- Emails include the business, plan/credit details, paid value or free-grant status, payment reference or grant reason, validity and a secure Billing link; they warn that AiFrogi never requests payment credentials or OTPs by email.
- Notifications run only after verified billing has committed. SMTP failure cannot reverse or falsely fail a successful plan/credit allocation and is recorded in the platform audit trail.
- Accepted notification events are idempotent by billing target, preventing duplicate client emails when payment verification is replayed.

## Billing email notifications — deployed

- Deployed release `billing-notifications-20260907` to AiFrogi VPS `187.77.188.146`; rollback is `/var/backups/aifrogi/billing-notifications-20260907.UdCyQW`.
- Production health passed and the verified plan, verified credit and Super Admin grant endpoints returned protected JSON responses.
- No payment, allocation or client email was created during deployment verification. The first real completed billing event will exercise the configured post-commit email path and record SMTP acceptance/failure.
- Cleared the Billing notification item from `docs/PENDING_VPS_CHANGES.md` after deployment verification.

## 10/10 readiness programme — three stages implemented locally

- Stage 1: added a canonical route manifest and release guard that requires the active AI Bot, Billing, Intelligence, Setup, Admin, widget and embed surfaces while failing if retired WhatsApp, campaign or legacy-preview routes return.
- Stage 2: failed billing confirmations now enter the existing idempotent automation queue for up to five attempts; exhausted retries create a high-severity platform incident. Admin Today shows retries, dead jobs, open incidents and billing-email failures from the previous 24 hours.
- Stage 2: added an isolated PostgreSQL restore-verification command that refuses the production database and requires a dedicated empty restore-test database.
- Stage 3: added a combined commercial-readiness suite covering route discipline, workspace/payment authorization, captured-payment gates, billing notification recovery, and a configurable 1–50 concurrent-request widget load probe.
- Token-saving verification used one combined focused gate and one final build: canonical route guard passed, three new commercial tests passed, 32 existing billing/widget/security regressions passed, TypeScript passed, and the clean 75-route production build passed.
- Added all three locally complete stages as one pending VPS readiness programme. Production is unchanged pending explicit package authorization.

## 10/10 readiness programme — deployed

- Deployed release `readiness-programme-20260907` to the AiFrogi VPS in one staged promotion; previous release was `billing-notifications-20260907`.
- Enforced canonical route discipline: 135 route source files checked, 24 retired WhatsApp/campaign/legacy-preview route files removed, zero retired routes remain, and all 13 required route groups are present.
- Activated billing-email retry/dead-letter incident handling, Admin Today operational-health visibility, isolated PostgreSQL restore verification, the commercial-readiness suite, and configurable widget load testing.
- Removed stray macOS AppleDouble (`._*`) metadata from application source directories so it cannot defeat route verification.
- Production readiness reports database, session secret, public URL, Meta webhook signature, and legacy inbound token as healthy. The public widget passed 20 concurrent loads with 20 HTTP 200 responses in 575 ms.
- Rollback snapshot: `/var/backups/aifrogi/readiness-programme-20260907.QfA5Uv`.
- Cleared `docs/PENDING_VPS_CHANGES.md` after successful production verification.

## 1,000-bot capacity assessment

- Saved the consolidated readiness milestone and capacity position in `docs/AiFrogi-10-of-10-Readiness-and-1000-Bot-Capacity-2026-09-07.md`.
- Current architecture is considered suitable for planning and controlled onboarding of 1,000 registered or mostly idle installed bots, but it is not certified for 1,000 simultaneous AI conversations.
- Live evidence is limited to 20 concurrent public widget renders. The 2-core VPS runs one PM2 application process; shared rate limiting, worker separation, database-pool measurement, progressive full-conversation testing, and overload recovery evidence are required before making a concurrency claim.

## Super Admin Capacity & Upgrade Advisor — implemented locally

- Added an Admin Today capacity summary and a detailed `/admin/capacity` page for the first operating target of 100 bots and 10,000 AI replies daily.
- The matrix reports configured/live bots, replies today and seven-day projection, peak replies per minute, recent active website sessions, p95 answer latency, answer error rate, automation queue depth/age, CPU/load, memory and disk.
- Green/amber/red rules identify the bottleneck and recommend targeted action. Provider latency and application errors do not incorrectly recommend a larger VPS; disk cleanup and worker separation are considered before general resource expansion.
- Four capacity-advisor unit tests, TypeScript and the complete 75-route Webpack production build passed.
- Added the completed local package to `docs/PENDING_VPS_CHANGES.md`; production remains unchanged until the next approved batched deployment.

## Super Admin Capacity & Upgrade Advisor — deployed

- Deployed production release `capacity-advisor-20260907`; previous release was `readiness-programme-20260907`.
- VPS staging passed 4/4 capacity tests, 3/3 commercial-readiness tests, canonical route verification, TypeScript and the complete 83-route Webpack build.
- Post-deployment readiness passed, all 13 required route groups remain present, zero retired route sources remain, and the public widget passed 20/20 concurrent loads.
- `/admin/capacity` is live and correctly protected by Super Admin authentication (HTTP 307 when unauthenticated).
- Rollback snapshot: `/var/backups/aifrogi/capacity-advisor-20260907.GSgZ72`.
- Cleared the Capacity Advisor package from `docs/PENDING_VPS_CHANGES.md` after successful promotion.
- Maintenance observation: the VPS `.env.local` contains malformed non-assignment text on line 13 and cannot safely be shell-sourced. The running Next.js service remains ready and reports its database check healthy; environment-file cleanup should be handled as a separate, carefully backed-up maintenance change.

## VPS environment-file repair — completed

- Created protected backup `/var/backups/aifrogi/env-local-before-cleanup-20260907-124404/.env.local` with mode `0600` before modification.
- Confirmed the `META_APP_SECRET` assignment contained 598 characters of accidental diagnostic-text contamination and no recoverable secret-shaped candidate; all available application/staging copies contained the same contamination.
- Because Meta/WhatsApp routes are retired but production security remains fail-closed, replaced the unusable value with a new 64-character cryptographically random server-only sentinel and retained `META_WEBHOOK_SIGNATURE_REQUIRED=true`.
- Restarted `lead-os-ai` with release `capacity-advisor-20260907`. Automatic restoration was prepared but not required.
- Verified zero malformed environment lines, correct secret shape without exposing its value, and full production readiness: database, session secret, public URL, Meta signature enforcement and legacy inbound-token checks all report `ok`.

## Razorpay billing webhook — deployed

- Deployed `https://app.aifrogi.com/api/billing/webhook/razorpay` as release `razorpay-billing-webhook-20260907` with a separate protected webhook secret.
- Added raw-body HMAC-SHA256 validation, public machine-ingress routing, Razorpay event-id deduplication, API read-back for captured payments, workspace/order/amount/currency checks, immediate plan or credit allocation, notification reuse, and audit records for captured, failed and processed-refund events.
- TypeScript and the complete production build passed. Unsigned ingress returned HTTP 401.
- A signed non-commercial `payment.failed` smoke event returned HTTP 200; replaying the identical event returned `duplicate: true` and made no customer billing change.
- Rollback snapshot: `/var/backups/aifrogi/razorpay-billing-webhook-20260907.xz9MFx`.
- Cleared the webhook package from `docs/PENDING_VPS_CHANGES.md`.

## Razorpay credential confirmation

- Rechecked the protected VPS `.env.local` without exposing values: exactly one `RAZORPAY_KEY_ID` and one `RAZORPAY_KEY_SECRET` exist; the Key ID is live-mode format and neither value has quotes or outer whitespace.
- Sent those exact trimmed values directly from the VPS to Razorpay’s official `POST /v1/orders` endpoint for an unpaid ₹399 diagnostic order.
- Razorpay returned HTTP 401 with `BAD_REQUEST_ERROR` and `Authentication failed`; no order was created and no payment was attempted.
- This rules out AiFrogi JSON parsing, domain routing and PM2 as the cause. The Razorpay Key ID/Secret pair is not accepted together—commonly because the secret is a webhook secret, belongs to another Key ID, or the API key was regenerated/revoked.
- TypeScript, nine focused billing/pricing/governance tests, scoped diff hygiene and the complete 75-route Webpack build passed after the recommendation layer was added.

## Final close-out — 2026-09-07

- Final production release: `razorpay-billing-webhook-20260907`; AiFrogi readiness is healthy.
- Completed the responsive website widget, reliable close/minimise behaviour, customer-controlled appearance and themes, reusable main-menu setup, persona intelligence, embed options, self-service installation evidence and checklist automation.
- Completed trial, plan, prepaid-credit and Super Admin promotional-credit workflows with client/admin transaction history, immediate verified allocation, email notifications, retry/dead-letter handling and operational visibility.
- Completed the 10/10 readiness controls, canonical route cleanup, removal of retired WhatsApp/campaign surfaces, Admin Today reliability cards and the Capacity & Upgrade Advisor for the 100-bot / 10,000-daily-reply target.
- Repaired the contaminated VPS environment entry using a protected rollback copy and retained fail-closed signature enforcement.
- Deployed the dedicated signed and idempotent Razorpay billing webhook; unsigned traffic is rejected and duplicate signed delivery was verified without altering customer billing.
- `docs/PENDING_VPS_CHANGES.md` is empty because every recorded package has been deployed.
- GitHub was deliberately not updated today. Local `main` is eight commits ahead of `origin/main` and the worktree contains extensive modified, deleted and untracked files. The next repository task is a secret scan, `.gitignore` review, artifact exclusion, full verification, controlled milestone commit and push to `git@github.com:asavaristays/aifrogi.git`.
