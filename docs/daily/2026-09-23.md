# 23 September 2026

- Audited the complete Markdown project record and traced the observed hotel-answer failures to shared Core behavior rather than missing tenant facts or TypeSafe: deep property context could evict the highest-ranked policy page, operation slots always inherited prior destinations, percentage spacing caused a false unsupported-number block, and unsupported qualitative additions such as free parking were not validated.
- With explicit user approval, implemented `CI-023` locally only. No tenant knowledge, frontend, TypeSafe setting, credit balance or production system was changed.
- Added deterministic answer-context priority and a replay trace containing retrieval question, selected context sources, proposed answer, final answer and validator violations. Deep entity context now requires an explicitly resolved tenant entity.
- Added current-question authority for general operation-capability questions, percent-spacing equivalence, and topic-specific validation for free/complimentary/included claims.
- Verification so far: focused tests 10/10, TypeScript pass, Sovereign 381/381, channels 232/232, production-hardening 126/126 and the 91-page webpack production build. Turbopack could not bind its internal sandbox port, so the established webpack production build was used and passed. Deployment remains pending fresh three-tenant Golden verification and explicit user approval.
- After explicit approval, created isolated production-derived candidate `/var/tmp/aifrogi-ci023-17e266c`; no live build or route changed. Candidate TypeScript and its complete 89-route webpack build passed. The fleet gate passed Core 30/30 and fresh Golden certification for Asavaristays, Castle Mandawa and Webtechnosys (3/3), including both privacy probes for each tenant.
- Important limitation: the current certification record retains question, expected outcome and disposition, but not the generated answer text. Asavaristays' ten-case bank also excludes the observed Kates rate, parking charge, general online-booking and cancellation questions. Therefore the green 3/3 result proves governed routing but is not sufficient human evidence that the reported answer regressions are corrected. No deployment was performed.
- With separate approval, replayed the same ten Asavaristays topics in sequence against the isolated candidate and captured exact answers/traces without creating a visitor conversation. Correct results included ₹22,500 plus 18% tax for Kates Adobe, no inherited Coorg destination for general online booking, the published cancellation policy, and the no-payment-verification boundary. One launch blocker remains: “Is parking free?” still answered that self-parking was “included as a property amenity,” although the approved source establishes an amenity rather than no-charge entitlement. `CI-023` is blocked and neither the security gate nor deployment was run.
- User approved the narrow validator correction and then two parser corrections exposed by replay. The final validator requires entitlement evidence close to the same topic, ignores negated phrases such as “not included,” and cannot parse the end of “incurs” as a rupee prefix. Focused tests passed 7/7, TypeScript, Sovereign 384/384 and production-hardening 126/126 passed.
- Final isolated replay passed all ten review topics: unsupported free parking was blocked to the safe response; breakfast “not included” remained answerable; the cancellation policy returned without a false numeric block; rate, capacity, Coorg directory, general booking and payment-verification behavior remained correct. The exact candidate then passed the complete 89-route webpack build and SEC-001 with 3 live tenants, 0 invalid credentials and 0 duplicate owners. No deployment was performed; explicit deployment approval remains required.
- After explicit approval, deployed only the CI-023 Core runtime files and exact verified build as release `c614c9b`. Before promotion, saved the previous build, affected sources and environment at `/var/backups/aifrogi/ci023-core-recovery-c614c9b-20260923`. Frontend, tenant knowledge, TypeSafe configuration and credits were not changed.
- Post-deployment evidence: PM2 is online with zero unstable restarts; localhost and public live/ready return status OK and release `c614c9b`; Asavaristays embed returns HTTP 200; all four promoted source hashes equal the isolated candidate; TypeSafe mode and all three routing flags remain off/false. The error log was last modified before the deployment restart, so no new runtime error was recorded during verification.
- With user approval to enhance shared Core for HotelGPT first, prepared `CI-024` locally. Added a deterministic 60-case bank: 40 universal visitor-error/Core cases and 20 HotelGPT cases. The bank covers intent and typo recovery, context replacement, claim grounding, incomplete answers, privacy/credential input, hotel routing, booking verification and stay facts.
- Shared corrections are deliberately narrow: hotel-policy typo routing, explicit “I meant …” correction replacing stale destination context, Hindi cross-guest-data blocking, password redaction without requiring punctuation, and rejecting/retrying visibly cut long model answers. No tenant knowledge, frontend, credits, TypeSafe, booking/payment authority or production configuration changed.
- `CI-024` evidence: bank 60/60, TypeScript pass, complete Sovereign suite pass, production-hardening 126/126 and channels 232/232. It remains undeployed pending the user's explicit review and deployment approval.
- After explicit approval, deployed `CI-024` as release `a30eccd`. The isolated production-derived candidate passed the 60/60 bank, TypeScript, the 89-route production build, Core 30/30, all three live tenants 3/3, and SEC-001. Rollback backup: `/var/backups/aifrogi/ci024-a30eccd-20260923`.
- Post-deployment: local and public readiness returned status OK with release `a30eccd`; Asavaristays embed returned HTTP 200; all four promoted runtime source hashes matched the candidate; PM2 was online with zero unstable restarts. The error log was last modified about twenty minutes before deployment, so the displayed historical validation entries were not new release errors. TypeSafe mode, gateway, hotel shadow and pre-send flags remained off/false. Commit `a30eccd` was pushed to `origin/main`.
- With explicit approval for Option 1, ran an operator-only TypeSafe shadow comparison over the same ten Asavaristays questions. It did not alter guest answers, create a public conversation, or enable production routing. Core again returned correct identity, destination, rate, capacity, Coorg directory, safe parking fallback, breakfast, online-booking, cancellation and booking-verification responses.
- TypeSafe was not suitable for live enablement from this result: it strongly agreed on several simple cases, but gave low-confidence partial judgments for the correct rate/capacity answers, skipped the safe parking fallback because its privacy filter treated first-person fallback wording as identifying, and flagged the cancellation answer after dropping part of the long evidence projection. Production TypeSafe flags therefore remain off; no rollback was required because live runtime behavior never changed.
- With user approval, implemented `CI-025` locally using n8n-style framed Core Intelligence rather than question-specific branching. Five reusable frames use a fixed detect → resolve → retrieve → authority → compose → verify sequence for multipart hotel answers, property facts, policies, booking boundaries and safe fallback.
- HotelGPT now decomposes multi-topic questions, verifies that each requested topic is addressed, resolves misspelled property names without generic-word collisions, and copies exact published rates/capacity without model reinterpretation or a live-availability claim. Frame guidance keeps policy categories and booking/payment states distinct.
- Added a deterministic five-dimension Core answer-quality score to Super Admin and a continuous 20-turn HotelGPT journey covering entity switching, multipart facts, policy, availability, booking, payment and privacy. Local evidence: focused 16/16, journey 20/20, Sovereign 390/390, production-hardening 127/127, channels 233/233, TypeScript and the 91-route Webpack production build passed. The default Turbopack build could not bind its sandbox-internal CSS worker port, so the established Webpack production gate was used and passed. TypeSafe, tenant facts, credits and marketing frontend remain unchanged. No deployment was performed.
- After explicit approval, deployed only `CI-025` as release `2ed4c0d`. The isolated production-derived candidate passed the packaged 14/14 focused tests, TypeScript, the 89-route Webpack build, Core 30/30, all three live tenants 3/3 and SEC-001. Rollback backup: `/var/backups/aifrogi/ci025-2ed4c0d-20260923`.
- Post-deployment: local and public readiness returned status OK with release `2ed4c0d`; Asavaristays embed returned HTTP 200; all eight promoted runtime source hashes matched the candidate; PM2 was online with zero unstable restarts. TypeSafe gateway, mode, hotel shadow, pre-send and staging flags remained off/false. No additional live bot-question test was started.
- After user-approved Chrome testing, the five failed/partial questions failed again: Kate’s multipart answer omitted bedrooms/bathrooms; an explicit Rohet Garh correction inherited Kate’s ₹22,500 rate; the Jodhpur-station question returned Rohet station; cancellation answers were repeatedly cut off; and a previous-guest request returned the public business number instead of a privacy refusal. No guest private data was exposed.
- With explicit approval, implemented `CI-026` locally as a reusable tenant/property-ID workflow: detect topics → resolve tenant → resolve stable property ID → retrieve only property-scoped evidence → apply authority → compose → verify ID, completeness, privacy and ending. It is not an Asavaristays-specific answer patch and is intended to scale to future HotelGPT tenants.
- `CI-026` also removes stale prior-property rates from explicit property corrections, adds exact bedrooms/bathrooms multipart extraction, distinguishes a requested landmark from a different published station, blocks incomplete published-claim fallback, increases policy-answer output space, and classifies previous/prior/last guest data requests as sensitive. Evidence: focused 17/17, Sovereign 391/391, production-hardening 94/94, channels 231/231, TypeScript and the 91-route Webpack production build passed. No production deployment, tenant facts, frontend, credits, TypeSafe or connector authority changed.
- After separate approval, deployed `CI-026` as release `5a47db6`. The production-derived stage passed focused 17/17, TypeScript, its 89-route build, Core 30/30, fleet 3/3 and SEC-001. Rollback: `/var/backups/aifrogi/ci026-5a47db6-20260923`. Public readiness was healthy, Asavaristays returned HTTP 200, PM2 was online with zero unstable restarts and all TypeSafe flags remained off.
- Live Chrome recheck of only the five prior failures: Kate’s four-field multipart still safely handed over rather than providing all four facts; explicit Rohet Garh switching no longer leaked Kate’s ₹22,500 rate but safely handed over because complete structured Rohet rate/capacity evidence was unavailable; the Jodhpur railway-station question correctly distinguished the published Rohet station; visual inspection confirmed the cancellation answer was complete (the accessibility snapshot alone capped the text); and the previous-guest request exposed no guest data but used generic handover rather than the intended explicit privacy refusal. No further behavior change was made.
- With explicit user authorization, deployed the HotelGPT in-stay access branch as release `hotelgpt-64adb36`. The journey is property QR → guest stay request → pending front-desk review → Owner/Admin approval or rejection → property- and checkout-bound guest session → existing Team Inbox communication → automatic expiry or manual revocation. It does not use a PMS, PIN, OTP or booking-reference lookup.
- The production-derived candidate passed 5/5 focused HotelGPT tests, TypeScript and the complete 90-route Webpack build locally and again in an isolated VPS stage. Encrypted database backup: `/var/backups/aifrogi/aifrogi-20260923T124506Z.dump.gz.enc`; source/build rollback: `/var/backups/aifrogi/hotelgpt-64adb36-20260923`.
- Migration `20260923180000_hotelgpt_guest_access` applied successfully. The live table has RLS enabled, RLS forced and one tenant-isolation policy. Local and public readiness are healthy on `hotelgpt-64adb36`; PM2 is online with zero unstable restarts, protected staff and invalid guest-capability probes return 401, an unknown stay slug returns 404, and deployed API/migration hashes match the tested branch. No hotel tenant or guest request was created during release verification; the fresh signup and complete human-operated journey remain the next acceptance test.
- Completed and deployed the signup-to-operations lifecycle as `hotelgpt-lifecycle-2a818e6`. A new hotel can select HotelGPT at registration; the governed profile explicitly controls the separate QR-based in-stay module; onboarding uses hotel-specific guidance; Super Admin go-live closes onboarding at 100%; and a live, enabled property receives its secured guest link and printable QR. Every public stay surface, the approval queue and resident-token use recheck both HotelGPT category and the module flag.
- The production-derived VPS candidate passed 34/34 focused lifecycle/profile tests, TypeScript and the complete 91-route Webpack build. Encrypted database backup: `/var/backups/aifrogi/aifrogi-20260923T131459Z.dump.gz.enc`; rollback package: `/var/backups/aifrogi/hotelgpt-lifecycle-2a818e6-20260923`.
- Migration `20260923193000_hotelgpt_stay_module` initially failed safely because the restricted application role does not own `BotProfile`; no live code had been promoted. The failed record was marked rolled back, the additive default-false column was applied through the local PostgreSQL table owner, and Prisma then recorded it applied. All existing bots—including all three existing STAY bots—remain disabled for the new module. Public readiness is green, PM2 is online with zero unstable restarts, unauthenticated QR/staff probes return 401 and unknown stay routes return 404.
- Fresh Camp Hornbill onboarding exposed `HG-001` at Step 1: tenant resolution completed under forced RLS, but the subsequent organization update ran after that transaction-local identity ended and Prisma returned `P2025`. Release `hotelgpt-onboarding-4c11bcc` keeps the full authenticated onboarding write inside an explicit tenant database context and validates the optional public email on both client and server. Production-derived TypeScript, scoped lint, 44/44 focused tests, the complete 91-route Webpack build, dashboard invariants, Core 30/30, live tenants 3/3 and SEC-001 passed. Live/ready are healthy, PM2 has zero unstable restarts, promoted hashes match, and no new production error-log entry appeared after restart. Rollback: `/var/backups/aifrogi/onboarding-step1-4c11bcc-20260923`. Final closure requires the authenticated user to retry Step 1 using a valid email address or leaving the optional email blank.
