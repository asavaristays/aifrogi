# 26 September 2026

- Completed the user-approved repository/cache inventory before deletion. The working tree already contained preserved generated Prisma, Readiness billing, commercial-launch and evidence/output work, so no broad source/output cleanup was performed.
- Removed only confirmed regenerable local caches: the 1.5 GB `.next` directory and `tsconfig.tsbuildinfo`. Retained `node_modules`, `.codex-build`, `tmp`, `output`, `outputs`, generated files, rollback/security evidence and all pre-existing modified/untracked work.
- Verified the Core production storage timer read-only. `aifrogi-storage-maintenance.timer` is enabled and active. Its last completed run was 25 September 2026 at 03:43 UTC; it selected 17 retained-cycle items, one cache and one oversized log and reduced disk use from 56% to 42%. Current disk use at review was 49 GB/97 GB (51%); the 26 September run was scheduled for 03:51:53 UTC. No production mutation was made.
- Investigated the GitHub/dependency-security concern with a live npm audit. The initial audit had nine advisories: four high and five moderate, with no critical finding. The first isolated candidate updated Prisma to 7.10.0, but was rejected before promotion because its tooling declares Node 22 while Core production runs Node 20. Prisma remains at the production-compatible 7.6.0; patched transitive overrides now pin `@hono/node-server` 1.19.15, `valibot` 1.4.2, `mysql2` 3.24.4 and `uuid` 11.1.1. This removes the Hono, Valibot, MySQL and UUID advisories without a production Node upgrade.
- Hardened `scripts/verify-dependency-security.mjs`: a registry/network failure or incomplete audit response now fails the gate as unknown instead of producing a false empty success.
- Remaining audit result is the reviewed high `deepmerge-ts` advisory through Prisma configuration tooling, surfaced as `deepmerge-ts`, `@prisma/config` and aggregate `prisma` entries. It is not loaded by the deployed application runtime; npm offers a breaking Prisma downgrade rather than a compatible fix. The existing `SEC-DEP-001` exception remains in force pending a compatible Prisma release/review deadline.
- Verification after the dependency update: Prisma schema validation passed, TypeScript passed, focused HotelGPT/In-Stay/security tests passed 17/17, dependency gate passed with zero unapproved high/critical findings, and the complete channel suite passed 273/273.
- After explicit approval, commit `3592988` deployed the Node 20-compatible dependency security package and resource-version reporting to Core production. The production-derived candidate passed the dependency gate, Prisma validation, TypeScript, SEC-001 focused 4/4 and its complete 93-route Webpack build. The initial Prisma 7.10 stage was rejected and never promoted.
- Rollback is `/var/backups/aifrogi/security-3592988-20260926.2SQoaH`. Local/public live and ready returned HTTP 200 with release `3592988`; database, session secret, public URL, webhook security and inbound compatibility were all OK. PM2 was online with zero unstable restarts. Camp Hornbill Pre-Stay and In-Stay returned HTTP 200, and all four promoted file hashes matched the verified candidate.
- Removed only the two exact temporary VPS stages and local regenerable build/release artifacts after verification. The live build, rollback backup, customer data, uploads and unrelated working-tree changes were preserved.
