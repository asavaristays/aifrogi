# AiFrogi daily work log — 2026-09-08

## GitHub milestone preparation

- Confirmed `origin` is `git@github.com:asavaristays/aifrogi.git`, local `main` has no incoming GitHub commits, and the eight existing local commits can be pushed without a merge.
- Audited the full dirty worktree and staged the consolidated product milestone: application source, database migration and generated client, tests, documentation, public customer downloads, and reusable verification tools.
- Expanded `.gitignore` to exclude reproducible acceptance output, duplicate generated PDF output, and one-off VPS deployment, promotion, rollback, diagnostic, and environment-repair utilities.
- Scanned the exact staged payload for live Razorpay, database, GitHub, OpenAI, Google, private-key, environment-file, and deployment-artifact exposure. Matches were limited to documented placeholders and isolated CI credentials.
- Regenerated Prisma using a non-connecting placeholder URL, normalized generator whitespace, and passed commercial readiness, canonical route checks, TypeScript, the complete core fixture suite, and the 75-page production build.
- Restricted ESLint to production application source; test fixtures remain checked by TypeScript and the core test runner. Fixed the remaining production lint errors before the milestone commit.

## Local artifact separation

- Created a Git-ignored `dump/2026-09-08/` archive so obsolete operational artifacts are no longer mixed with active source files.
- Moved one-time VPS deployment/promotion/rollback/diagnostic utilities into `vps-scripts/` and reproducible acceptance plus duplicate generated output into `generated-output/`.
- Kept active application source, reusable verification scripts, public customer downloads, tracked guide PDFs and build caches in their correct locations.

## Storage and Codex token optimization

- Measured production storage: root disk 65% used; backups 6.0 GB/57 items; temporary AiFrogi artifacts 1.2 GB/175 items; stages 1.2 GB/4; PM2 logs 450 MB; npm cache 729 MB; Next.js cache 484 MB.
- Added a dry-run-first, bounded-retention maintenance command plus a daily systemd timer; deployment remains queued in `docs/PENDING_VPS_CHANGES.md`.
- Documented the durable token policy: related 2–3 change batches, focused checks, one final build, one deployment, compact evidence and one changed-journey verification.
- Deployed the maintenance command and daily systemd timer to VPS `187.77.188.146`; the first dry run selected 196 cyclic items, two caches and two oversized logs.
- Applied the reviewed cleanup successfully: root usage fell from 63 GB/65% to 57 GB/59%, leaving 41 GB free. Retention now holds 31 backup items, 10 temporary artifacts and two build stages.
- Verified the timer is enabled and active and the AiFrogi application remained healthy with HTTP 200. Cleared the pending deployment ledger after verification.

## Agentic lead qualification v1

- Implemented a small-scale agentic lead journey for website bots with approved capture and qualification capabilities.
- Added deterministic commercial-intent activation, progressive fact collection, bounded repeat questions, transparent scoring, priority recommendation and optional explicit contact consent.
- Connected qualification progress to the widget and persisted the result for a generic Team Inbox summary; no external action or additional model call was added.
- Added focused regression coverage and queued the locally verified Intelligence batch for VPS deployment.
- Tightened the final step after mentor review: name plus valid mobile number and explicit consent are required before a lead becomes handoff-ready; email alone is not accepted as the callback contact.
- Validation passed: 118/118 channel tests, TypeScript, lint with zero errors, whitespace checks and the 75-route production build. The first sandboxed build was blocked by Turbopack loopback-port permission; the permitted local rerun compiled successfully.
- Deployed source-only release `agentic-lead-qualification-20260908` with no database migration. The first promotion attempt automatically rolled back after a verification URL used a non-canonical Webtechnosys slug; previous release health was confirmed before retrying.
- Rebuilt and promoted using canonical slug `webtechnosys-ai-agency-e5da22`. Production readiness passed for database, session secret, public URL, webhook signature and legacy inbound token; Webtechnosys bot and Team Inbox routes passed.
- Rollback backup: `/var/backups/aifrogi/agentic-lead-qualification-20260908.Nr7q02`. Pending deployment ledger cleared after verification.
- Completed the callback-display decision rule: the highlighted name/mobile consent form is withheld until score 60 with a known need and timeline or budget evidence; low-value or exhausted discovery does not request mobile details.
- Added the standing agentic completion gate: no new agentic capability begins until the current capability's logic, safety, visibility, tests, deployment verification and pilot measurement are complete.
- Validated the completed threshold with 120/120 channel tests, TypeScript, lint with zero errors and the 75-route production build.
- Deployed `qualified-callback-threshold-20260908` with no migration. Production health passed all readiness checks; the canonical Webtechnosys bot and Team Inbox routes passed. Rollback backup: `/var/backups/aifrogi/agentic-lead-qualification-20260908.bOLgSf`.
- Cleared the pending deployment ledger. The capability is implementation-complete and now awaits pilot evidence before score/question tuning or another agentic capability begins.

## Support desk clarity redesign

- Reviewed the live client Support page in Chrome and confirmed the confusing hierarchy: the long temporary-access audit preceded tickets and ticket creation.
- Reorganized the client journey around a clear help-centre header, primary ticket history and an always-visible new-ticket form; popular guides remain secondary.
- Moved customer-controlled temporary access and audit history into a collapsed privacy section used only when support requests diagnostic access.
- Upgraded super-admin Support to a focused queue with Open, Urgent, Waiting and Resolved totals; queue filters; urgent-first ordering; explicit columns and clearer empty states.
- Preserved ticket lifecycle, tenant isolation, secret protection, audited access and email notification behavior. Focused lint, TypeScript, 4/4 support-operation tests and the 75-route production build pass.
- Added the completed local package to `docs/PENDING_VPS_CHANGES.md`; production remains unchanged until deployment authorization.
- Deployed release `support-desk-clarity-20260908` to VPS `187.77.188.146` after explicit authorization. The isolated VPS build passed with 83 routes, and production readiness passed database, session, public URL and webhook checks.
- Verified `/support` and `/admin/support` remain authentication-protected and `/api/support/tickets` rejects unauthenticated requests. Reloaded the authenticated client Support page in Chrome and visually confirmed the new ticket-first hierarchy and collapsed privacy panel.
- Rollback backup: `/var/backups/aifrogi/support-desk-clarity-20260908.PRHGxz`. Cleared the pending VPS ledger after successful production verification.

## Conclusive support interaction

- Added explicit support read/activity ownership so dashboards distinguish a new response from ordinary open-ticket history.
- Client Today now displays active AiFrogi Support responses with ticket status, latest response summary and a direct View/Reply or Review Resolution action.
- Super-admin Command Center now prioritizes new client replies, urgent/high-impact requests and overdue tickets; tickets waiting on the client do not create a false admin action.
- Visiting client Support marks the workspace ticket stream viewed; opening an admin ticket marks it viewed by support without modifying the ticket activity timestamp.
- Added an additive database migration and focused regression coverage. Validation passed with 122/122 channel tests, focused ESLint, TypeScript, whitespace checks and the 75-route production build.
- The package is locally complete and queued in `docs/PENDING_VPS_CHANGES.md` pending VPS authorization.
- Deployed release `conclusive-support-interaction-20260908` after explicit authorization. The isolated production build passed with 83 routes and migration `20260908063000_support_response_visibility` applied successfully.
- The first two migration invocations correctly stopped before promotion because Prisma did not inherit the PM2-only database URL. The final guarded runner passed that existing environment directly without exposing or changing the credential; production remained unchanged during both pre-promotion failures.
- Production readiness passed database, session, public URL and webhook checks. `/dashboard`, `/admin`, `/support` and `/admin/support` remain authentication-protected; the ticket API rejects unauthenticated requests.
- Visually verified the authenticated Webtechnosys Today page in Chrome. With zero support tickets, no false Support response panel appears, which matches the intended actionable-only behavior.
- Rollback backup: `/var/backups/aifrogi/conclusive-support-20260908.1TmtZz`. Cleared the pending VPS ledger after verification.

## Website-bot Reports accuracy

- Audited the live Reports page and confirmed it used real database leads, transcripts and AI-operation records with no mock fallback.
- Corrected misleading semantics: the former “Today” view used all-time activity and campaign/read-rate guidance did not match the website-bot product.
- Added real Today, 7-day, 30-day and all-time periods, website-only message filtering, response coverage, qualified-lead and consented-contact metrics.
- Period-scoped actions and verified outcomes, and retained only completed outcomes in recorded-value reporting.
- Queued the corrected Reports package and the shared `/team-inbox` TopBar link in `docs/PENDING_VPS_CHANGES.md` pending validation and deployment authorization.
- Validation passed: 125/125 channel tests, focused ESLint, TypeScript, whitespace checks and the 75-route production build.
- Deployed source-only release `website-reports-accuracy-20260908` after explicit authorization. The isolated VPS build passed with 83 routes and production readiness remained healthy.
- Verified authenticated Reports in Chrome with real period controls, `Last 7 days` labelling, website visitor messages, bot/team replies, qualified leads, consented contact capture and website-focused recommendations.
- Confirmed the shared Inbox header now routes to `/team-inbox`. Rollback backup: `/var/backups/aifrogi/website-reports-accuracy-20260908.VnCbyy`.
- Cleared the pending VPS ledger after production verification.

## Reports period selector contrast

- Reviewed the supplied Reports screenshot and confirmed that a shared link color overrode the white selected-period label.
- Corrected the active period contrast and added an explicit accessible current-page state; queued the small CSS package for validation and deployment.
- Validation passed: focused ESLint, TypeScript, 3/3 reporting tests, whitespace checks and the 75-route production build.
- Deployed release `reports-selector-contrast-20260908` after explicit authorization. The isolated VPS build passed with 83 routes and all four Reports period routes remained protected and reachable.
- Verified the active `7 days` selector in Chrome with computed foreground `rgb(255, 255, 255)` against background `rgb(23, 33, 30)`.
- Rollback backup: `/var/backups/aifrogi/reports-selector-contrast-20260908.y7q9HN`. Cleared the pending VPS ledger.
