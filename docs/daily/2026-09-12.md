# AiFrogi work log — 12 September 2026

## `TI-010` Tenant Intelligence fact factory

- Reviewed the supplied Tenant Layer checklist as product guidance and kept the implementation entirely within Tenant Intelligence.
- Reused the existing atomic approval, versioning, rollback, conflict, expiry, readiness and missing-answer infrastructure instead of duplicating it.
- Added shared website page classification and typed business-fact extraction with source authority, confidence, freshness and canonicalisation.
- Added client-visible website fact review with conflict status and missing-information punch list; extracted website material remains a draft and cannot silently become live bot truth.
- Added focused coverage for page classification, Indian contact normalisation, authority/conflict handling and no-invention punch-list behaviour. The combined Tenant regression passed 17/17, TypeScript passed, targeted ESLint reported no errors, and the complete 85-route webpack production build passed.
- The default Turbopack build could not bind its internal process port inside the restricted local sandbox; the supported webpack build completed successfully, confirming application compilation, TypeScript and every route.
- Full Sovereign test execution still contains environment-dependent HTTP fixture failures returning 503; these are broad pre-existing fixture failures, not failures in the isolated Tenant factory tests.
- No Core Intelligence, production tenant data or VPS state was changed in this batch.

## Production deployments

- Deployed the previously pending Core release separately as `ci010-timezone-20260912`. The production-derived stage passed 24/24 focused checks, TypeScript and all 85 routes; public readiness is healthy. Rollback: `/var/backups/aifrogi/ci010-timezone-20260912.ELuwsB`.
- Deployed Tenant release `TI-010` separately as `ti010-tenant-fact-factory-20260912`. The production-derived stage passed 17/17 focused checks, TypeScript and all 85 routes; public readiness is healthy, PM2 is online with zero unstable restarts, and the Webtechnosys standalone bot route returns HTTP 200. Rollback: `/var/backups/aifrogi/ti010-tenant-fact-factory-20260912.Mnnzvy`.
- The initial Asavari route guess was not accepted as evidence. Read-only production lookup shows `asavari-stays-67142e` belongs to a removed organization and `asavaristays-703624` remains onboarding; no tenant lifecycle data was changed during this deployment.
- Cleared both deployed items from the pending VPS ledger. Human review of the generated tenant profile on two live bots remains the next evidence gate.

## `UI-002` light widget close control

- Confirmed from the Asavari Stays screenshot that the generic launcher rendered its close icon using white-on-light styling, making it visually disappear.
- Replaced the tenant-specific light/dark styling with shared widget theme variables for icon, border and control surface.
- The fix remains launcher-only and does not change standalone navigation, bot intelligence or tenant colour configuration.
- Deployed as `ui002-close-control-20260912` after explicit authorization. The production-derived stage passed 15/15 lifecycle tests, TypeScript and the complete 85-route webpack build.
- Production readiness is healthy, the launcher embed returns HTTP 200 and the compiled static assets contain the new global close-control style. Rollback: `/var/backups/aifrogi/ui002-close-control-20260912.z0o8yW`.
- Cleared `UI-002` from the pending VPS ledger after verification.

## `CI-017` truthful Today activity matrix

- Confirmed that the shared client Today matrix was presenting delivery and read percentages inherited from messaging-channel semantics, although the current product is a website bot.
- Confirmed that activity totals were all-time even though the screen and labels said Today.
- Replaced the shared matrix for every tenant with: current conversations needing reply, conversations today, visitor messages today, and AI/team replies today.
- Today activity now uses the existing India-day reporting boundary; the reply queue deliberately remains cross-day so an older unanswered customer cannot disappear at midnight.
- Removed decorative percentage bars from raw count cards because they suggested an unsupported performance score.
- TypeScript and all 178 channel/product tests passed locally.
- Deployed as `ci017-today-matrix-20260912` from a production-derived isolated stage. The matching focused production gate passed 7/7 and the complete 85-route build passed.
- Production health is green, PM2 is online with zero unstable restarts, and authenticated Asavari verification shows exactly the four new metrics with truthful zero activity for 12 September.
- The broader production test baseline still contains unrelated legacy assertions for retired messaging and older billing wording; these were not altered or treated as `CI-017` failures.
- Rollback: `/var/backups/aifrogi/ci017-today-matrix-20260912`.

## Core robustness programme — `CI-004`, `CI-011`, `CI-013`

- Extended the existing Core capabilities without creating tenant-specific copies or mixing Tenant Intelligence into the release.
- Added a pre-delivery visitor-answer gate that rejects internal/governance wording, known robotic deflections, duplicated phrases, excessive responses and premature budget/contact qualification.
- Centralized missing-answer recovery into one category-aware path that routes to the correct team, offers private consented callback capture, includes the approved business phone when available and retains the existing persisted lead/Team Inbox handover.
- Added a mandatory server-side 30-question Core certification to Super Admin go-live. A failed or incomplete Core suite blocks activation; a pass is shown to Super Admin and written to the onboarding audit in the same lifecycle transaction.
- Tenant readiness remains independent and mandatory: approved essential topics, safety, freshness/conflict rules, a recorded customer-question test, plan status and any required connector evidence are still checked separately.
- Local verification passed: TypeScript, 178/178 channel/product tests, 4/4 new robustness tests and the complete 85-route webpack build. The broad environment-dependent suite still contains pre-existing HTTP-fixture and unrelated dirty-worktree failures; these were not hidden or treated as launch evidence.
- Deployed as `core-robustness-20260912` from a production-derived isolated stage. The focused answer-quality and website-bot security gates passed 16/16 and the complete 85-route webpack build passed.
- Production readiness is healthy and `lead-os-ai` is online after restart. Rollback: `/var/backups/aifrogi/core-robustness-20260912`.
- The broader production-derived channel suite exposed unrelated legacy assertions for older billing copy and a fixed-time greeting; these baseline failures did not touch the six promoted runtime files and remain recorded rather than hidden.

## `CI-018` reusable tenant-owned connector API control

- Founder approved a reusable Super Admin API-installation control for all current and future bot personas; this is a recorded exception to the roadmap's immediate Tenant-only pilot sequence, limited to connector configuration and verification—not autonomous booking execution.
- Extended the existing persona connector plan instead of building an Asavari-specific integration. Tenant Owner/Admin can securely save a public HTTPS API host, authentication method, replace-only encrypted secret and operation paths, then run an authenticated health check from Setup.
- Super Admin cannot configure or read credentials; it sees only the API host and test evidence, then controls lifecycle approval and live enablement.
- Connector lifecycle remains fail-closed: saving resets it to authorised, a passing health test moves it only to connected, and mapped/live stages cannot be selected without healthy evidence. Actual booking writes still require provider sandbox, idempotency, authoritative read-back and separate approval.
- Deployed as `ci018-connector-api-20260912` after a full PostgreSQL dump and exact application backup. The production-derived connector gate passed 3/3 and all 86 routes built.
- Migration `20260912190000_bot_connector_api_control` was applied by the verified PostgreSQL table owner and recorded by Prisma. Live verification confirmed all seven connector fields, the isolated credential table and a 401 response for unauthenticated tenant connector access.
- Production readiness is healthy and `lead-os-ai` is online. Rollback and database backup: `/var/backups/aifrogi/ci018-connector-api-20260912`.
- Added and deployed `ci018-payment-verification-20260912` for the missing tenant-managed payment-verification API path. It is deliberately distinct from booking status so the runtime cannot treat a created booking as proof of payment. The focused connector gate passed 3/3, all 86 routes built, production readiness is healthy and the live source contains the new field. Rollback: `/var/backups/aifrogi/ci018-payment-verification-20260912`.
