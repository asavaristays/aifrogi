# Pending VPS changes

This file contains only changes that are complete locally but not yet deployed to the VPS.

## Reports period selector contrast

- Force the active period label to white against its dark selected background.
- Mark the selected period with `aria-current="page"` for assistive technology.
- Validation complete: focused ESLint, TypeScript, 3/3 reporting tests, whitespace checks and the 75-route production build pass.
